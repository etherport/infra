name: Ansible Proxmox

on:
  workflow_dispatch:
    inputs:
      playbook:
        description: 'Playbook to run'
        required: true
        type: choice
        options:
          - proxmox-setup
          - proxmox
      action:
        description: 'Action to perform'
        required: true
        type: choice
        options:
          - check
          - apply
      allow_reboot:
        description: 'Allow reboot if needed'
        required: false
        type: boolean
        default: false

permissions:
  contents: read

env:
  WORKING_DIR: infra/ansible

jobs:
  ansible:
    name: Ansible ${{ github.event.inputs.playbook }} (${{ github.event.inputs.action }})
    # Lifecycle runner (gh-runner VM, outside the K8s cluster) — required
    # so Proxmox/cluster-management workflows still work if the cluster
    # is offline. `homelab-runner` runs inside K8s = chicken-and-egg.
    # No `container:` block: the gh-runner VM doesn't have Docker. Install
    # ansible on the runner directly via apt.
    runs-on: [self-hosted, lifecycle]

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install ansible
        run: |
          sudo apt-get update
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y \
            python3 python3-pip python3-venv ansible openssh-client

      - name: Setup SSH key
        env:
          ANSIBLE_SSH_KEY: ${{ secrets.ANSIBLE_SSH_KEY }}
        run: |
          mkdir -p ~/.ssh && chmod 700 ~/.ssh
          printf '%s\n' "$ANSIBLE_SSH_KEY" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -H pve.wind.etherport.net >> ~/.ssh/known_hosts 2>/dev/null || true

      - name: Run Ansible playbook (check mode)
        if: github.event.inputs.action == 'check'
        run: |
          ansible-playbook \
            -i inventory/wind/ \
            playbooks/${{ github.event.inputs.playbook }}.yml \
            --check \
            -v
        working-directory: ${{ env.WORKING_DIR }}

      - name: Run Ansible playbook (apply)
        if: github.event.inputs.action == 'apply'
        run: |
          EXTRA_VARS=""
          if [ "${{ github.event.inputs.allow_reboot }}" = "true" ]; then
            EXTRA_VARS="-e allow_reboot=true"
          fi
          ansible-playbook \
            -i inventory/wind/ \
            playbooks/${{ github.event.inputs.playbook }}.yml \
            $EXTRA_VARS \
            -v
        working-directory: ${{ env.WORKING_DIR }}

      - name: Summary
        if: always()
        run: |
          {
            echo "## Ansible Proxmox"
            echo ""
            echo "- **Playbook**: ${{ github.event.inputs.playbook }}"
            echo "- **Action**: ${{ github.event.inputs.action }}"
            echo "- **Allow Reboot**: ${{ github.event.inputs.allow_reboot }}"
          } >> $GITHUB_STEP_SUMMARY
