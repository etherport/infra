# macOS
.DS_Store

# Claude Code
.claude/

# Terraform
**/.terraform/
**/*.tfstate
**/*.tfstate.*
**/*.tfvars
!**/*.tfvars.example
**/terraform.tfvars.local
**/*.tfvars.local
**/terraform.tfvars.*.local

# Packer
**/*.pkrvars.hcl
!**/*.pkrvars.hcl.example
**/.packer_plugins/
**/packer_cache/

# Ansible/Kubespray artifacts
**/*.retry
**/inventory/**/artifacts/
**/inventory/**/credentials/*
!**/inventory/**/credentials/*.sops.yaml

# Kubeconfigs
**/admin.conf
**/*.kubeconfig
**/.kube/
**/kubeconfig/

# Secrets (commit templates only)
**/secrets/*
!docs/setup/secrets/*
**/*secret*.yaml
!**/*secret-template*.yaml
!**/*.secret-template.yaml
!**/*.sops.yaml

# Explicitly ignored secret files
platform/kubernetes/route53-ddns/base/secret.yaml

# Config files that may contain credentials (prefer SOPS)
**/alertmanager-config.yaml
**/credentials.yaml
!**/*.sops.yaml
# Generic catch-alls for raw private-key material. All real secrets are
# SOPS-encrypted now (*.sops.yaml — see ~20 files across the repo) so we
# should never have raw keys committed; these patterns are defense in depth.
**/*.key
**/*.pem
**/id_rsa
**/id_rsa.pub
**/id_ed25519
**/id_ed25519.pub
**/wg0.conf
**/wg1.conf
**/wg-private-*.key
.vscode/
