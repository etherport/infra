apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Automation controller config
  - automation.yaml
  # cue (active-dev app) — image automation IS the deploy trigger (private image)
  - cue-ghcr-secret.sops.yaml
  - cue.yaml
  # LLM Services
  - ollama.yaml
  - open-webui.yaml
  # Infrastructure
  - technitium.yaml
  # Applications
  - wikijs.yaml
  - plex.yaml
  - home-assistant.yaml
  # Backup Tools
  - rclone.yaml
  # Base/Utility Images
  - base-images.yaml
