# Outstanding work — completed-items archive (extracted 2026-07-01)

Full text of ✅-done tracker items extracted from [`../outstanding-work.md`](../outstanding-work.md)
on 2026-07-01, the second such extraction (first: [`completed-2026-H1.md`](completed-2026-H1.md),
2026-06-24 — many entries below are the one-line stubs that pointed there; they are preserved so
every ID stays grep-able). Also preserves the tracker's retired top-matter: the 2026-06-23 aws-s3
adversarial-review fix table, the 2026-05-29 "Next-up checklist", the 2026-05-27 → 06-15
"Recently completed" blocks, and the 2026-06-10 full-repo-review summary. Items keep their original
IDs and full text; relative links were re-based for this directory.

> Historical record — don't link here from active code; link to the current state in
> `outstanding-work.md` or the relevant runbook instead.

---

## Retired top-matter (historical sections from the live tracker)

_The three still-open AB-* rows of the adversarial-review table also remain in the live tracker._

## 2026-06-23 — aws-s3 NAS→S3 backup: adversarial review + fix set

First adversarial review of the `platform/kubernetes/backups/aws-s3` app (full
narrative + per-finding rationale in [`session-log.md`](../session-log.md);
plan/findings at `~/.claude/plans/pure-floating-kernighan.md`). Full fix set
shipped this session:

| ID | Finding | State |
|---|---|---|
| **AB-H1** | Lock released by a non-owner job (defeats the mutex → concurrent `--delete`) | ✅ ownership-gated `release_lock` |
| **AB-H2** | End-of-run degraded block (`local`/unset var) flips a warning-run into a FAILED Job | ✅ fixed; can't change exit status |
| **AB-H3** | Delete guard measured a dry-run; real `--delete` was a separate unbounded sync (TOCTOU wipe) | ✅ re-assert source health before the destructive sync (conservative) |
| **AB-M2** | Transient verification HEAD → false FAILED (no retry) | ✅ retry `head-object` on transient |
| **AB-M3** | 24h stale-lock < long initial sync → concurrent run | ✅ 48h, env-overridable |
| **AB-M4** | `daily-report` SA/RBAC missing from git (drift); dead `ghcr-creds` ref | ✅ captured SA/RBAC; image is public, ref removed |
| **AB-M5** | Approve token echoed to pod logs | ✅ redacted (REQUIRE_CF_EMAIL left off — split-horizon) |
| **AB-L1** | `validate-existing-backups.sh` false-"corruption" on multipart + report-upload AccessDenied | ✅ composite-aware + `reports/` prefix |
| **AB-L2/L3/L5a-d** | dead code, stale README IAM JSON, fail-open dry-run, no ephemeral-storage limit, composite 8MB assumption, `mktemp` | ✅ all addressed |
| **AB-L4** | Mutable `:main` image tag for a delete-capable workload | ⏳ deferred (sha-pin after tonight's approved run; `:main` kept so tonight gets the fixes) |
| **AB-H3b** | Fuller manifest-driven delete (drive deletes from the measured set) | ⏳ deferred follow-up to the conservative re-assert |
| **AB-L6** | "Object Lock" claimed in README but no `object_lock_configuration` in TF | ⏳ operator verifying out-of-band (believed enabled) |
| **AB-H5** | Verify path (GNU parallel `{1} {2}` column substitution) mangled keys with spaces+special/multibyte chars → bogus `--bucket` → `HeadObjectFailed` → run FALSELY marked FAILED (9/20,813 on the 2026-06-24 backups run; **all 9 objects present in S3, no data loss**) | ✅ fixed `a700b3f` — pass whole `bucket<TAB>key` as one opaque arg + `IFS=$'\t' read` split; validated 9/9. See session-log cont.8 |

## Next-up checklist (updated 2026-05-29)

Active execution order. The zone migration + switch ACLs + Kopia decom +
personal-web CI all landed this session (see "Recently completed" below).

| # | Item | State | Action |
|---|---|---|---|
| 1 | **M18/M36 MetalLB BGP** ✅ **DONE + VERIFIED 2026-06-12** | Was: MetalLB L2 mode → UDM `.5`/`.71` IP-conflict alerts | **A** 209 NICs → **B** 201→UDM-routed → **C** UDM↔MetalLB eBGP (8/8 sessions, 5 VIP /32s via BGP/ECMP) → **D** L2Advertisement removed (`69a332b`) = **BGP-only**. VIPs verified reachable intra-201 (kube-proxy) + cross-VLAN (BGP); only raw ICMP-to-VIP stops (harmless). M36 root cause (ARP/MAC churn) gone. ✅ **Verified 2026-06-12 via Loki** (`{host="udm"}`, 14d incl. ~2d pre-fix overlap back to 05-29): **0** `.5`/`.71` conflict/duplicate alerts. Closed. Runbooks: bgp-phase-{a,b,c}. UDM BGP is UI-managed (no API) — durable via git FRR config + controller backup; recheck → M58. Trusted-zone for 201 → M56. |
| 2 | **M15-M17 Twilio Talk** | 911 addr / orphan DID / SIP UDP→TLS+sRTP | M15 (911) safety-first; M16 release DID; M17 encryption |
| 3 | **M53 CF token scoping** ✅ **DONE 2026-06-13 (personal-web)** | Was: account-scoped token shared infra↔personal-web | personal-web CI now uses a **zone-scoped** token (`cloudflare-personal-web-tf`: grahamsmith.net/smithforsb.com/stopthecastle.com only, + DNSSEC + Dynamic Redirect) — GH secret updated, verified `cloudflare-dns` plan clean. It can no longer reach etherport. **Remaining (optional):** re-scope the *infra* token to etherport-only (needs a `sync-secrets.py` re-bake of the mini's SOPS bundle). Headless CF for the personal-web agent: local-only `secrets/cloudflare.sops.yaml` on the mini (gitignored per personal-web convention; holds the **master** token — owner-okayed — so the agent can run CF terraform/M54 without op/1P). |
| 4 | **M54 smithforsb redirect IaC** 🟢 **UNBLOCKED** | CF Single Redirect still manual in dashboard | M53's scoped token has `Dynamic Redirect: Edit` → personal-web agent can now codify it (`cloudflare_ruleset` in `cloudflare-dns/smithforsb.tf`, import the live rule, verify 301). |
| 5 | **M48/M49/M50 UNAS+Protect IaC** | UI-only | Per-device API keys → Ansible playbooks + coverage audit |
| 6 | **M69 Cloudflare provider v4→v5** ✅ **APPLIED + VERIFIED 2026-06-13** | Was: 51 resources incl. Tunnel + Zero Trust Access (breaking rewrite) | Migrated live (headless via SOPS token): `39 imported, 11 benign in-place, 0 destroyed`; post-apply plan = **No changes**. cloudflared 0 restarts; Access gating intact (approve/grafana/wiki 302 externally). PR **#66** reconciles `main`→v5. personal-web migrated first as proving ground. Plan/log: [`cloudflare-provider-v5-migration.md`](cloudflare-provider-v5-migration.md). |
| 7 | **L21 GCP auth → WIF** ✅ **DONE 2026-06-13** | Was: `terraform-google` red since inception (empty `GCP_SA_KEY`, never applied) | GitHub→GCP **Workload Identity Federation** live — no static key (mirrors H29). WIF pool/provider/SA `gh-actions-terraform` bootstrapped in `homelab-infra-497414`; workflow on `google-github-actions/auth@v2`; both projects imported; CI dispatch = **"No changes"**. Enabled `iamcredentials`/`cloudresourcemanager`/`cloudbilling` APIs + declared `billing_account` on the CF-SSO project to kill import drift. `terraform-google` now green. Doc: [`gcp-oidc-wif-l21.md`](gcp-oidc-wif-l21.md). |

**Recently completed (this session, 2026-05-27 → 29):**
- ✅ BGP **Phase A** — 209 (vsan) NIC added to w1-4 + gpu1 (DHCP .100-.104); kubelet NFS to the UNAS now egresses `enp6s23` at L2. Caught + fixed the source-IP gap: UNAS NFS export ACL extended to the 209 IPs on all 9 shares (UI, not IaC — re-apply on rebuild). Runbook updated with the "additive path ≠ no behaviour change" lesson.
- ✅ Velero `kube-system-daily` PartiallyFailed (recurring ≥4d) root-caused — node-agent lacked control-plane tolerations so it skipped cilium's `tmp` fs-backup on cp1-3. Fix: `nodeAgent.tolerations` in velero values (Flux). No data loss (resource backup was 371/371; only an ephemeral emptyDir was un-backed-up).
- ✅ M32 firmware channel → `release` + M34 site-wide auto-upgrade → off (IaC: `udm-firmware-policy.yml`, commit `ea08bea`)
- ✅ M30 — UDM zone migration COMPLETE (IoT + Infrastructure/212 + Security/205 custom zones; Phase 4 skipped)
- ✅ M52 — L3-switch ACL IaC applied + verified (5 ACLs on Switch Rack PoE)
- ✅ M33 rsyslog → Loki; M31 backup + OOB verified; 14 firewall groups (`3271cea`)
- ✅ Kopia decommissioned (deployment + CF tunnel + monitoring + 205 GiB Ceph reclaimed)
- ✅ personal-web repo CI (GH Actions + secrets; all 3 modules plan clean)
- ✅ CF API token fixed (user-rotated) + etherport DS record re-imported + `ignore_changes` pinned
- ✅ M30 Phase 1: VLAN 212 → Infrastructure zone (commit set through `05d635c`); all 18 clients healthy
- ✅ M30 §7 questions resolved + L3-routing topology corrected (Switch Rack PoE routes 201/202/209/210)

**Recently completed (2026-05-30 → 31):**
- ✅ BGP migration **A→D COMPLETE** — node 209 NICs, 201→UDM-routed, UDM↔MetalLB eBGP, L2Advertisement removed. M18/M36 resolved.
- ✅ vpn-local wg-failover self-heal (`reset-failed`); advisor vpn-local SSH-IP fix (.5→.15); sops+age in the CI runner (SOPS playbooks now run in CI).
- ✅ cw2loki "unknown" report bug fixed (cronjob `time()` scalar parse); appliance probes (UDM/Protect/UNAS) scraping; Velero node-agent on all 8 nodes.
- ✅ Networking prod-review (`networking-prod-review-2026-05-31.md`): P1.2 default-deny verified, P1.3 DDNS confirmed working, NetFlow decided (superseded by unifi-poller).
- ✅ Doc consolidation: archived 21 done/superseded docs; this tracker trimmed 684→~270 lines (full snapshot in `archive/outstanding-work-snapshot-2026-05-31.md`).

**Recently completed (2026-06-01 → 10):**
- ✅ **Headless ops host** — always-on Mac mini (`10.10.202.101`, tailnet `100.79.165.113`) provisioned as the Claude Code Remote Control box: full kubectl/terraform/sops/ansible with **no 1Password at runtime** (commits `00f365f`, `14e1f07`). AWS creds baked into SOPS (`scripts/render-aws-credentials.sh`); proxmox tf creds via `scripts/tf-proxmox.sh`; scoped `trusted-admin-clients → Management` UDM rule (applied from the mini). Procedure: `docs/setup/headless-ops-host.md`.
- ✅ Doc accuracy pass (2026-06-10): README (headless host, M56 zone split, MetalLB BGP-not-L2) + `firewall-zones.md` (the new trusted-admin-clients rule) updated.

**Recently completed (2026-06-13 → 15):**
- ✅ **M69 / L21 / M53** — Cloudflare provider v4→v5 (PR #66), CI→GCP WIF, zone-scoped CF token (details in their tier entries + planning docs).
- ✅ **localtuya migration** — all 8 Tuya devices moved cloud→local (localtuya), entity_ids/names/automations preserved; trial-expiry-proof. (HA-side, not in repo IaC.)
- ✅ **Protect webhooks → HA fixed** (commits `9fd7c50`, `840f6c0`). Root cause was a Protect Alarm Manager bug (`ERR_INVALID_IP_ADDRESS` on hostname webhooks), NOT DNS/network. Added a dedicated plain-HTTP Traefik `webhook` entrypoint (`:8088`, `PathPrefix(/api/webhook/)` → HA:8123); Protect uses `http://10.10.201.70:8088/api/webhook/<id>`. Full narrative in [`session-log.md`](../session-log.md).
- ✅ **HA motion-light automations → `mode: restart`** so re-triggered motion resets the off-timer (was `single`).
- ✅ **Agent handoff system** — added root `CLAUDE.md` (entry point + operating model + maintenance rules) + this file's companion [`session-log.md`](../session-log.md) (narrative journal).

## Full-repo review — 2026-06-10

A 5-dimension review (IaC consistency · k8s/Flux · docs · security-hardening ·
CI/supply-chain) ran 2026-06-10. New items carry IDs **H29–H34 / M60–M68 /
L16–L20**. Headline corrections + top gaps:

- ⚠️ **H3 was overstated** — the "audit-only CNPs already deployed" claim is
  FALSE. There are **zero** NetworkPolicy/CiliumNetworkPolicy objects in the repo
  and Cilium `policyEnforcementMode` is unset (allow-all). H3 reframed below.
- **Top hardening gaps (new):** 22 CI workflows on long-lived static AWS keys, 0
  OIDC (**H29**); 0 SHA-pinned actions / 0 digest-pinned images (**H30**);
  orphaned `claude-admin-temp` IAM policy with `iam:*`/`DeleteUser` on `*`
  (**H31**); auto-remediation SA has cluster-wide `delete pods/PVCs` (**H32**); no
  secrets-rotation runbook + single age key (**H33**).
- **What's genuinely solid** (don't re-spend): no plaintext secrets in tree/history;
  SOPS pre-commit gate works; `kube_encrypt_secret_data: true`; EBS encryption;
  minimal regex-scoped public tunnel surface; cloudflared pod well-hardened;
  broad+alerted backups; resource-prefixed `terraform-*` IAM policies.

📋 **Detailed, ready-to-execute implementation plans for H3 + H29–H34** (steps,
exact commands, sequencing, verification, rollback) live in
[`hardening-plan-2026-06-10.md`](hardening-plan-2026-06-10.md). Recommended order:
H34 → H31 → H29 → H30 → H33 → H32 → H3.

---

## HIGH — completed items

### ✅ H3. NetworkPolicies — ALL 5 TARGET TIERS ENFORCED + drop-alerting live 2026-06-23  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ H35. Cilium/kubespray durability hardening (post-incident) — DONE 2026-06-15  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ H29. CI AWS auth → GitHub OIDC — CUTOVER COMPLETE 2026-06-12  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ H30. Supply chain — SHA-pin Actions + digest-pin images — DONE 2026-06-24
- **Source:** review 2026-06-10. **0** of 104 `uses:` were SHA-pinned; **0** of 22 image refs `@sha256`-pinned; in-house images pull `:main`; SOPS binary curl'd unverified in 3 workflows.
- ✅ **Done 2026-06-11 (safe/non-rolling):** added `helpers:pinGitHubActionDigests` to `renovate.json` (next Renovate run opens a reviewable PR SHA-pinning all actions); authored `.github/actions/setup-sops/action.yml` (pinned + **checksum-verified** SOPS install, sha256 `5488e32b…` from the official checksums).
- ✅ **Actions SHA-pinned 2026-06-13:** Renovate PR **#62** ("pin dependencies") merged — **all 111 `uses:` now digest-pinned** (was 0). (The pin PR was unblocked by raising `prConcurrentLimit` 5→8, and went green once L21's WIF fixed `terraform-google`.)
- ✅ **SOPS checksum-verify — DONE:** all 3 TF workflows (`terraform-drift-detection`, `terraform-aws-us-east-1`, `terraform-regional-vpn`) already use the `./.github/actions/setup-sops` action (no more unverified curl). Verified 2026-06-24.
- ✅ **Image digest-pinning — DONE 2026-06-24** (`206417b`→Wave-2): added `digestReflectionPolicy: Always` + `interval` to **all 13 `ImagePolicy` objects** → Flux automation rewrites manifests to `tag@sha256:…` (immutable). Rolled out **staged**: canary `blackbox-exporter` (validated end-to-end) → **Wave 1** non-critical (python-alpine/-slim, busybox, velero-plugin-aws, rclone, open-webui, ollama, wikijs) → **Wave 2** sensitive (technitium DNS, cloudflared tunnel, home-assistant, plex). Whole fleet rolled clean; **DNS resolved + tunnel stayed up** throughout (technitium STS + cloudflared are 2-replica → one-at-a-time). NB churn: hourly digest re-resolution rolls a pod only when a tag's digest actually changes (rare for fixed semver tags).
- 🟢 **In-house images (aws-s3-sync, backup-approval, ai-advisor, etc.) — ACCEPTED on `:main`/CI-rebuilt:** these are OUR images in OUR ghcr (we control the build), `imagePullPolicy: Always` + a path-filtered CI rebuild on push, so the upstream-tag-mutation threat that digest-pinning addresses barely applies. Bringing them under Flux image-automation + digest is **optional future work**, not required for H30. (cue-api stays `:latest` during dev — see below.)
- 🟢 **`cue-api` `:latest` — intentional during dev (owner 2026-06-17):** the dangling `$imagepolicy` marker on cue-api (no matching ImagePolicy → floats on `:latest`) is **accepted for now** — cue is in active development / testing mode, so always pulling latest is desired. **Do not pin/fix this yet.** Revisit when cue-api goes to production (then either add a real `cue-api` ImagePolicy or pin by digest, and remove/repair the marker). Tracked here so the supply-chain sweep doesn't "fix" it prematurely.
- **DONE:** Actions SHA-pinned (111), SOPS checksum-verified, all upstream images digest-pinned (13 ImagePolicies). Residual is the deliberate in-house-`:main` + cue-api-`:latest` choice (optional future digest-pin).

### ✅ H31. Redesign `claude-admin` (break-glass, broad-but-not-escalation) + full IAM audit — APPLIED 2026-06-11  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ H32. Scope auto-remediation RBAC — confined destructive verbs (2026-06-11)  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ H33. Secrets-rotation runbook + 2nd age recipient (single-key blast radius) — DONE 2026-06-15  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ H34. Narrow + log the `trusted-admin-clients → Management` firewall rule (2026-06-11)  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ H36. Flux reconciliation alerting (silent GitOps-wedge gap) — DONE 2026-06-17  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ H37. Proxmox host firewall (host mgmt plane) — ENFORCING (default-deny) 2026-06-17  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ H40. Technitium DNS replica made disposable (recreatable from code) — DONE 2026-06-19  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ M82. Terraform → CI-only (devbox drops standing AWS/PVE creds) — DONE 2026-06-24
- **Decision (owner 2026-06-24): CI-only.** Executed: (1) added the missing **`terraform-proxmox-firewall`** workflow (`.github/workflows/`, modeled on terraform-proxmox-sdn, self-hosted `lifecycle` runner, `PROXMOX_TOKEN_*` secrets, **TF 1.15.5** — state was written by 1.15.5, so 1.14.3 would refuse it) → **every TF stack now has CI**. (2) **Removed the standing `~/.aws/[homelab]` key from the devbox** (the AWS key stays only in SOPS-encrypted + GH secrets; `aws_access_key_id` confirmed still in the SOPS bundle for on-demand). (3) CLAUDE.md §4 updated: devbox = **Actions:write PAT (M92)** to dispatch CI + the SOPS age key (needed for headless `sops -d`), **no standing AWS/PVE creds**. Rare local-debug TF → `scripts/render-aws-credentials.sh` (throwaway) + `scripts/tf-proxmox.sh`. **Verify:** the firewall workflow's first plan run (auto-triggered on the push) should be green / 0-diff — check the Actions UI; ensure `PROXMOX_TOKEN_ID/SECRET` GH secrets exist (the sibling proxmox workflows use them, so they should).
- **Superseded context below (the decision options).**
- **Source:** 2026-06-19. To apply the H37/Ceph firewall fix from devbox (dev sessions moved off the mini — M81), devbox was made **TF-capable**: `terraform` 1.15.5 + `aws` CLI + the homelab AWS profile rendered from SOPS + PVE token via `scripts/tf-proxmox.sh`. This **expands devbox's blast radius** (now holds the homelab AWS keys + PVE token) — counter to the original "devbox = no TF/creds" design and the ZT direction ([[M71]]/[[H38]]). Owner accepts for now (it was needed). **Decide:** keep it (codify into `devbox.yml`), or re-home TF to the mini / a CI workflow (note: the proxmox-firewall stack has **no CI workflow** — adding one would let firewall changes apply via the self-hosted runner instead of any workstation). **Effort:** S (decision) + S–M (codify or build the workflow).
- **Practical path to drop the creds (2026-06-21):** execute **all** TF via **CI** — every stack now has a workflow (AWS via OIDC; proxmox/unifi/cloudflare on the self-hosted `lifecycle` runner with PVE/CF/UDM creds as **GitHub secrets**, NOT on devbox), and the devbox can trigger them via the dispatch PAT ([[M92]]). Add a `terraform-proxmox-firewall` workflow (the one stack still lacking CI) to close the gap. devbox would then keep only the **Actions:write PAT** (small blast radius) instead of AWS admin + PVE token + age key. Rare local-debug TF → short-lived creds on demand (`aws sso login`, throwaway PVE token), never standing ones.

### ✅ M84. Velero PVB "prepare timeout" under nightly load — node-agent dataPathConcurrency — FIXED 2026-06-19  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ M85. AI advisor — suppress diagnosis email for already-resolved (transient) alerts — DONE 2026-06-19  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ M86. UNAS SSD-cache member drop (NVMe APST hang) → md-degradation alerting — DONE 2026-06-19  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ M87. cloudwatch-to-loki — baked image (runtime pip eliminated) — DONE 2026-06-20  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ M89. pve-ipmi TargetDown — H37 firewall missing the IPMI exporter allow — FIXED 2026-06-20  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ M90. CI drift detection extended to all TF stacks (was AWS-only) — DONE 2026-06-20  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ M92. devbox GH dispatch PAT — DONE 2026-06-20  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ M94. S3 delete-guard + Cloudflare-Access approval flow — DONE + LIVE 2026-06-21  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ M95. rclone gdrive/onedrive sync hardening — DONE 2026-06-21  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ M96. iCloud Photos dedup (NAS + S3) — DONE 2026-06-21  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ M97. Tighten over-broad terraform-homelab IAM grants (+ orphan-policy audit) — DONE 2026-06-22  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ M98. iam-apply workflow — remote apply of manually-managed IAM policies — DONE 2026-06-21  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ M99. rclone-onedrive: exclude OneDrive Personal Vault — DONE 2026-06-21  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ M100. Mac mini photos-export observability (cluster side) — DONE 2026-06-22  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ M88. OneDrive → NAS backup (rclone-onedrive) — DONE 2026-06-20  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ M101. VM auto-update gaps — devbox unmanaged, security-only policy, no node patch path — DONE 2026-06-24  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ M103. `cairn` — native iCloud Backup Agent (replace the bash suite) — **CUTOVER DONE 2026-06-25** — repo: [sparked-diamond/cairn](https://github.com/sparked-diamond/cairn)
- **Source:** owner 2026-06-24. The mini's ~8 bash backup jobs work but are 8 LaunchAgents / 8 envs (per-job bugs: sops-PATH, FDA, contention) and copy-paste to extend. Replace with **one configurable app**: pick what to back up, the NAS destination, schedules, and notifications via YAML. **Decision: Swift, macOS-native** (not cross-platform) — backups only run where iCloud lives; the NAS is just a mounted dest. Native frameworks (PhotoKit / EventKit / Contacts) **eliminate the failures we fought** — iCloud CalDAV/CardDAV rate-limiting (the M80 calendars outage), the app-password+sops dependency, osxphotos/vdirsyncer fragility. `rsync` stays the mirror engine (subprocess), orchestrated by Swift `TaskGroup` (the parallel sharding); no Go/FFI needed. Notes/Safari/Drive stay file/SQLite (no public API). Four notifiers (Prometheus/Pushgateway, email-SES, ntfy/Pushover push, webhook), one-shot + daemon, launchd install, per-app TCC + code signing.
- **Own repo** (standalone product): [sparked-diamond/cairn](https://github.com/sparked-diamond/cairn), cloned at `~/code/cairn`. Design + code live entirely in that repo ([`cairn/DESIGN.md`](https://github.com/sparked-diamond/cairn/blob/main/DESIGN.md)); nothing about cairn's design lives in infra. **Reporting = Prometheus/Pushgateway ONLY — no secrets, no email/phone in the app** (Alertmanager routes alerts from the metrics, as for everything else). Metrics kept drop-in compatible with the current dashboard/alerts; **migration is incremental + reversible** (run in parallel to a staging dest, diff per category, cut over one at a time, retire each bash agent only after parity). **Mini = dev/build/test AND deploy** (Xcode being installed for `swift test` + code signing). **infra's role:** document `cairn`'s mini deployment (launchd/TCC/signing runbook) once built. **Effort:** L. **Status (2026-06-24):** **M1–M5 + M6-half done** (27 tests green). M1 mirror engine + M2 guards/sharding; **M3 all native sources** — contacts (verified **2,049 vCards = exact parity**), notes/calendars/reminders (`store`: consistent `sqlite3 .backup` + auto-discover), messages (in-place chat.db + 48 GB Attachments); **M4 photos** (osxphotos wrapper, in-place, byte-for-byte → no S3 churn) + the **network-resilience foundation** (supervised runner w/ stall-watchdog + tree-kill, SMB→sparsebundle→APFS self-heal, resumable retry); **M5** status/`cairn_health` heartbeat/`cairn tcc`; **M6** `install` (launchd LaunchAgents) + `run --due` (catch-up, idempotent) + quit-Photos-before-runs (kills the interactive-Photos `(-1)` dialogs). **Decisions:** wrap osxphotos (not native PhotoKit) for full metadata + zero re-upload; keep+harden the network sparsebundle (not SSD/icloudpd); calendars via `store` (not EventKit→.ics). **Key finding:** photos library is on a sparsebundle-on-SMB (`/Volumes/PhotosLib`), seed at ~10% (4,572/~44k originals); the network throws real I/O errors on hot DB reads — exactly what the resilience absorbs. **Remaining:** CI signed release + the operational cutover (grant cairn.app FDA/Photos via VNC → cairn owns the headless seed → retire bash jobs per-category). **Deploy runbook:** [`../runbooks/cairn-deployment.md`](../../runbooks/cairn-deployment.md). **Metrics cutover (decided 2026-06-25, cairn `0985323`):** a verified cross-repo audit found cairn was NOT drop-in (bash uses name-mangled `*_backup_*`/`photos_export_*`; cairn emitted bare names). Chose a **clean label-based schema** — cairn now emits `cairn_backup_*{job,instance="mini"}` + `cairn_photos_*` with the bash suite's rich metrics PORTED (missing resolvable/unavailable split, orphans, summary_parsed success-gating, messages/attachments split; new `safari` + `file` source) — and the **Grafana dashboards/alerts get rewritten to match** (keep `mini-health.sh` for the host heartbeat). Infra-side hand-off prompt (executed, now archived): [`archive/cairn-cutover-infra-prompt.md`](cairn-cutover-infra-prompt.md) (dashboards, alerts incl. the ICloudBackupEmpty rc-gate fix, Alloy log paths, Pushgateway stale-group cleanup, reversible per-category sequence). **✅ CUTOVER COMPLETE (2026-06-25):** all categories migrated to cairn + verified; 4 bash agents retired (booted out + disabled, reversible); dashboards/alerts rewritten to `cairn_*` + Flux-reconciled; 18 stale bash Pushgateway groups deleted; `mini-health` EXPECT updated; **zero S3 churn**. Findings (now in code + README §6 + the runbook): launchd responsible-process FDA; calendars/reminders live in the group containers; photos = local mode (osxphotos `--use-photokit` needs the System library); stall-watchdog relaxed for silent SMB phases; osxphotos collision tolerance; TCC FDA probe → `chat.db`. **Docs:** cairn `README.md` (install/config/metrics/gotchas), `DESIGN.md` (in production), [`../runbooks/cairn-deployment.md`](../../runbooks/cairn-deployment.md). **✅ CI signed release DONE 2026-06-26 (cairn M7):** a `vX.Y.Z` tag triggers `release.yml` (cairn repo) → build/test/**sign** (cairn-codesign identity, leaf `541075…` pinned so the mini's TCC grants survive a drop-in) + publish a GitHub release; `v0.1.0` shipped + verified. `scripts/setup-ci-signing.sh` (one-time, VNC) sets the 3 repo secrets. **M103 fully complete.**

### ✅ M102. Cue public internet access + Web Push — DONE 2026-06-24  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ H38. Internal identity-aware access (Authentik IdP) — kill "internal = trusted" — SSO PASS COMPLETE 2026-06-23  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ H39. Velero fs-backup incident (wedge since rev5) + AI-advisor email-flood — RESET + HARDENED 2026-06-18, residual RESOLVED 2026-06-22  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

## MEDIUM — completed items

### ✅ M5. Velero schedule kustomization ordering + ResourceQuota CR — DONE 2026-06-17 (see [[H39]] — rev5 deploy of this change correlated with a fs-backup wedge; resolved by reset, not revert)  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ M15. Twilio Talk: 911 emergency address — RESOLVED (verified at carrier 2026-06-24)
- Source: task #20. **Verified via the Twilio API 2026-06-24:** the primary DID `+19094142433` (PN `PN2b496425…`, re-acquired 2026-05-26 — the runbook's old "FAILED" was on the retired PN `PN7b83e…`) shows **`emergency_status: Active`**, bound to address **`AD1fe171…` = 843 GREENBRIAR DR SKYFOREST CA**, which is **`validated: True, emergency_enabled: True`**. So 911 delivers correct location at the carrier (SIP-trunk 911 uses the DID's emergency address; the UniFi Talk UI `emergency_address/list=[]` is the app's cosmetic view, not carrier delivery). The Twilio TF `emergency_address` var matches (843 Greenbriar Dr / Skyforest / CA / 92385) → no drift. **No apply was needed** — the live state is already correct + the TF intends it.
- ⏳ **TF-hygiene follow-up (not a safety item):** confirm `twilio_api_accounts_addresses.primary` + the primary DID are imported into the twilio TF state, so a future `terraform apply` reuses (not duplicates) the address. Do during the next twilio-stack touch (M16/M17). See `docs/runbooks/unifi-talk.md` §"Known issues" #2.

### ✅ M53. Mint zone-scoped CF API tokens (de-couple infra ↔ personal-web) — DONE 2026-06-17  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ➡️ M54. Codify smithforsb.com redirect (CF Single Redirect) as IaC — MOVED to personal-web repo (2026-06-17)
- **Closed here:** ownership of the `smithforsb.com` redirect moved entirely to the **personal-web** repo (it's a personal-web concern, codified alongside `cloudflare-dns/smithforsb.tf` there). No longer tracked in this infra repo. ID retained for grep history.

### ✅ M55. UniFi telemetry exporter (unifi-poller) — LIVE  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ M56. Servers/201 → Trusted zone + Management split — COMPLETE 2026-05-31  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ M42. UDM WireGuard cleanup + VPN-zone tighten — DONE 2026-06-24
- **Source:** carried forward via `docs/architecture/firewall-zones.md` cross-reference. Original archive ID was M14 but that's now reused for the s3-sync SSL probe item above. ID rotated to M42 to disambiguate.
- ✅ **WG peer list already clean:** `wg0_regional_peers: []` in `wireguard.yml` (vpn-mumbai removed/documented post-M38). No stale homelab-side peers.
- ✅ **VPN-zone allows tightened 2026-06-24** (owner: keep the UDM backup WG tunnel + `Vpn` zone as break-glass for cluster/PVE VPN loss + future tunnels e.g. Teleport, but scope it): `Vpn → Trusted/Management (all)` → `(admin)` = **TCP on `Vpn-Admin-Ports`** (22/53/80/443/6443/8006), logged. Applied live from devbox (create-only playbook → additive create + API-delete of the 2 old `(all)`; verified only `(admin)` remain). Resolves firewall-zones anomaly #3 (unused zone → full-LAN-reach if populated).

### ✅ M106. PVE vzdump of the cp1 VM stalls etcd daily → cluster lease-storm — DONE 2026-06-30 (operator excluded k8s-cp1/2/3 from the vzdump job)
- **Source:** overnight alert-storm post-mortem 2026-06-30 (multi-agent triage). **Root cause of a DAILY ~10:05Z cluster blip:** a host-level Proxmox **vzdump (snapshot-mode) backup of the k8s-cp1 VM** stalls its virtual disk (`sda`) for ~10 min → freezes that etcd member + the **leader apiserver's writes** (apiserver write p99 **48-58s**, etcd backend-commit p99 2.8s) → ~10 leader-election leases expire cluster-wide → scheduler/controller-manager/cilium-operator/csi-rbd-provisioner/kyverno restart + both technitium pods recreate → the email storm (TechnitiumDNSDown, KubePodCrashLooping, etcd gRPC errors, TargetDown). **etcd quorum was NEVER lost** (`etcd_server_has_leader=1` throughout); self-recovers ~10:25Z. Signature confirmed 06-27/28/29/30 (cp1-only, sda-only IOPS→0.1 at ~10:05Z). cp1 is the single `controlPlaneEndpoint` (`.50`), so its stall is maximally disruptive.
- **Fix (needs PVE — NOT IaC'd, devbox has no PVE creds):** the CP VMs don't need a full-VM vzdump — **etcd is already backed up host-side** (`etcd-backup.yml` + `EtcdSnapshotStale` alert) and the cluster rebuilds via kubespray. So **exclude `k8s-cp1/2/3` from the vzdump job** (or move them off snapshot-mode / add IO-throttling/fleecing / relocate etcd's data dir onto a vzdump-excluded volume). Validate: the daily 10:05Z `sda` stall disappears on `10.10.201.50:9100`. Consider IaC'ing the PVE backup schedule afterward.
- **Resilience follow-ups (optional):** apiserver `--etcd-servers` already lists all 3 members (verified ✅). Could raise leader-election `leaseDuration`/`renewDeadline` for the core controllers so a <15-min single-member stall doesn't cascade — but eliminating the stall is the better fix than masking it. **Effort:** S (PVE backup-job change).

### ✅ M107. Weekly doc/IaC drift audit — built + hardened — DONE 2026-06-30
- **What:** an unattended **weekly live-anchored doc/IaC drift audit** on the devbox (`doc-drift-audit.{sh,service,timer}`, Mon 14:37) — headless `claude -p` compares docs + IaC against LIVE state (kubectl/UDM-API/`ip route`), **auto-fixes high-confidence DOC drift** (commit+push), reports IaC drift (never auto-applies), and reports via a `doc-drift` GitHub issue. Complements the continuous CI detectors (M90 + the 2026-06-30 suite) by catching the "201-class" doc-vs-reality drift a `terraform plan` can't see. Detail: [`../../infra/devbox/README.md`](../../../infra/devbox/README.md).
- **Hardening shipped this session (commits `2c73fe3`→`b0e7af7`):**
  - **Issue posting** via a tiny `post-doc-drift-issue.yml` (its `GITHUB_TOKEN` has issues:write; the dispatch PAT is Actions:write-only, 403s on /issues) — open-on-drift / close-on-clean.
  - **Scoped permissions** (dropped `--dangerously-skip-permissions`): `--permission-mode default --settings doc-drift-audit-permissions.json` — read anything / write only docs+READMEs+CLAUDE.md + the in-repo gitignored `.audit-state/` artifacts / mutate nothing but a GitHub dispatch; **deny** terraform+ansible apply, kubectl mutations, rm/destructive-git, sops-encrypt. Matcher facts: `$()` + `VAR=x cmd` env-prefix are rejected → wrapper exports `SOPS_AGE_KEY_FILE`, secret+curl ops go through `audit-helpers.sh`; `awk` removed (write-hole). Empirically validated (allowed reads run; `kubectl delete`/`terraform` blocked) + a full clean end-to-end run.
  - **Email every run** (clean or drift) via SES **SMTP** (devbox has no IRSA; creds from the alertmanager SOPS secret) — multipart HTML with **deep-link approve buttons** (#40): each actionable item links to that apply workflow's GitHub Run page (no new public endpoint — proportionate).
  - **Off-box failure/missed-run alert:** node_exporter `--collector.textfile` (added to `base.yml`, applied to the devbox) + `doc_drift_audit_last_{rc,success_timestamp}` → `DocDriftAuditStale`/`NoMetrics` (critical→email) + `DocDriftAuditFailed` (warning) in `02-external-alerts.yaml`. Verified live (metric scraped, rules loaded). The audit is a systemd USER unit so node_exporter's systemd collector can't see it — textfile is the right signal.
- **Status:** DONE + enabled (timer active). Self-maintaining from here.

### ✅ M108. Google Places API (New) key for Cue Find-food — DONE 2026-06-30
- **What:** Cue's "Find food" feature calls Places API (New) server-side (`CUE_GOOGLE_PLACES_KEY`, gated behind `CUE_FIND_FOOD`, off). IaC in the existing `infra/terraform/google/` stack (`51c634d`): `google_project_service` enables `apikeys.googleapis.com` + **`places.googleapis.com`** (NB: NOT legacy `places-backend.googleapis.com`) on `homelab-infra-497414`, + restricted `google_apikeys_key.cue_places` (API-restricted to `places.googleapis.com`, no referrer/IP — server-side calls). Sensitive output `cue_places_key`.
- **Done:** user granted `roles/serviceusage.apiKeysAdmin` to the WIF SA (manual, devbox has no GCP creds) → `terraform-google.yml` apply (run 28479202922, **3 added / 0 destroyed**) → key read from state (rendered temp AWS creds, M82 throwaway, then shredded) → `sops set` into `cue-api/03-secret-app.sops.yaml` `CUE_GOOGLE_PLACES_KEY` (`bd41ad2`). `envFrom: secretRef: cue-app` injects it.
- **✅ Feature LIVE (2026-06-30):** cue side shipped the 🍽 Food tab; `CUE_FIND_FOOD: 'true'` flipped on inline in `cue-api/01-deployment.yaml` (`4073445`), Flux-reconciled, rollout healthy. Verified: pod has `CUE_FIND_FOOD=true` + `CUE_GOOGLE_PLACES_KEY` (39ch); `/food/nearby` is live (401 auth-required in-cluster — NOT 503/404, so the gate passed + key present). Final restaurants-load check = the operator's authed browser at `cue.etherport.net/ui/food`. The app must call Places API (New) `https://places.googleapis.com/v1/...` (the key is restricted to that; legacy `maps.googleapis.com/maps/api/place` is rejected). Runbook C: [`../../infra/terraform/google/README.md`](../../../infra/terraform/google/README.md).

### ✅ M47. UDM Network App modernization — API-key auth — DONE 2026-06-24 (auth-swap; URL migration deferred)
- ✅ **Auth swapped to X-API-Key 2026-06-24:** the API key already existed (`udm_api_key` in the SOPS ops bundle). **Verified** `X-API-Key` authenticates the legacy `/api/` + v2 `/v2/api/` endpoints the playbook uses (GET firewall-policies + firewallgroup → 200; bad key → 401). `udm-firewall.yml` now builds `udm_headers` from `X-API-Key` when `UDM_API_KEY` is set (preferred), **falling back to the username/password login** otherwise — non-breaking. Validated both paths via `--check` (key path: login skipped, GETs ok; fallback: login runs, GETs ok; both `failed=0`). `ansible-unifi.yml` passes `UDM_API_KEY` (empty until the secret's added → falls back).
- ⏳ **Follow-up (one-time, you):** add a `UDM_API_KEY` GitHub secret (value = the same key in SOPS) so CI uses the key; then drop the username/password login fallback from the playbook + the `UDM_USERNAME/PASSWORD` GH secrets. **URL migration** to `/proxy/network/integration/v1/...` stays deferred (firewall groups have no Integration equivalent yet; revisit at UniFi Network 10.2+). Scoping runbook: `docs/runbooks/archive/udm-network-app-modernization.md`.
- **Original entry below.**

### ⏳ M47-orig. UDM Network App modernization — API key + Integration API
- **Source:** 2026-05-26 audit while working Twilio Talk (#22). Found that `infra/ansible/playbooks/udm-firewall.yml` still uses the legacy username/password + `POST /api/auth/login` cookie auth flow against `/proxy/network/v2/api/...` and `/proxy/network/api/s/...`. UniFi Network Application ≥10.1.84 ships an official **Integration API** at `/proxy/network/integration/v1/...` with API-key auth (`X-API-Key` header), which is more durable + key-rotatable than the cookie dance.
- **Scope:**
  - User creates UDM API key in console → Control Plane → Admins → Create API Key. Stored in 1P as `unifi-udm-api` (API Credential category: `credential` = key value). Least-privilege scope; only widen if a playbook requires it.
  - Add `udm_api_key` to `infra/ansible/inventory/group_vars/all/secrets.sops.yml`.
  - Update `udm-firewall.yml` (+ any future UDM playbooks) to swap login flow → `headers: { X-API-Key: "{{ udm_api_key }}" }`.
  - Where Integration API covers the resource, migrate the endpoint (`/proxy/network/integration/v1/sites/{site}/...`); where it doesn't, keep `/proxy/network/v2/api/...` but with the new auth header (still supported).
  - Add `UNIFI_UDM_API_KEY` GH Actions secret for CI runs.
- **Effort:** ~half day. Mirror the auth-modernization pattern used elsewhere.
- **Source:** memory `reference_udm_zone_policy_api.md` for current state.

### ✅ M48. UNAS — config-as-code state backup (2026-05-31)  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ M49. UniFi Protect — config backed up to S3 (verified 2026-05-31)  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ M50. UDM/UNAS/Protect IaC coverage — assessed 2026-05-31  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ⛔ M59. Dedicated LB VLAN for MetalLB VIPs — PARKED + backed out 2026-05-31
- **Decision:** not worth it. The foundation (VLAN 215 + `lb-vlan` pool + `lb-vlan-bgp` advertisement) was built, but because Servers/201 + LB would share the **same `Trusted` zone**, moving VIPs onto a 215 subnet buys **no segmentation** — only cosmetic addressing. And a zero-downtime cutover isn't cleanly possible: MetalLB rejects two IPv4s on one service (`same address family`), so the dual-home attempt failed and briefly scared the live ingress/BGP. Spending live-traffic risk on address hygiene is a bad trade.
- **Backed out fully:** removed the TF network (`unifi_network.loadbalancers` → VLAN 215 destroyed), the MetalLB `lb-vlan` pool + advertisement, and the migration runbook. All VIPs stay on 201 (BGP-advertised, working). The `.5` DNS pin decision still stands (it's a network-wide contract; stays on 201 regardless).
- **If revisited:** only worth it if LB VIPs get their **own** zone (DMZ-style for service front-ends) — then re-create 215 as its own zone, not bundled with `Trusted`.

### ✅ M60. Secret-scanning in pre-commit + CI (gitleaks) — 2026-06-11  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ M62. etcd snapshots → offsite (S3) + freshness alert (supersedes L15) — DONE 2026-06-17  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ M65. Terraform consistency — version pins, DRY, hardcoded IDs — DONE 2026-06-17  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ M66. Cilium pod-to-pod encryption — ENABLED (WireGuard) 2026-06-17  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ M67. Reconcile dead/contradictory Velero alerting config — DONE 2026-06-17  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ M68. Documentation consolidation + accuracy pass — DONE 2026-06-17  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ M72. Pod Security Admission: audit/warn → enforce — DONE 2026-06-28 (tail closed)
- **Source:** zero-trust assessment 2026-06-17. `policy-baseline/` ran PSA in audit/warn only (Phase 1). Same per-ns opt-in model as H3.
- ✅ **Enforcing `baseline` (15 namespaces):** the 14 tier=data app namespaces (authentik, auto-remediation, backups, cloudflare-ddns, cloudflared, cloudwatch-to-loki, cue, dns, ollama, postgres, rclone, traefik, unifi-cert-sync, wikijs — done 2026-06-02) **+ `flux-system` (2026-06-24, `5a52b54`)** via the `namespace-pss-labels.yaml` patch (verified live; flux controllers healthy — baseline not restricted to avoid any future-upgrade wedge on the engine, though dry-run was restricted-clean). `unifi-poller` = enforce **restricted**.
- ✅ **Exempt by design (legit elevated pods — verified via `--dry-run=server`):** `monitoring` (node-exporter host-ns/hostPath/hostPort, alloy), `tailscale` (privileged proxy), `home-automation` (HA privileged), `gpu-operator-system` (GPU operator host/privileged), `velero` (node-agent hostPath) → kept **audit/warn=baseline** (new violations still flagged). `blackbox`/`metallb`/`wireguard` = enforce privileged.
- ✅ **Residual CLOSED 2026-06-28 (commit `93589a2`):**
  - **3 infra namespaces enforced → baseline:** `cert-manager`, `cnpg-system` (via the central
    `namespace-pss-labels.yaml` patch — their Namespace manifest **does** ship in
    `helm-releases/{cert-manager,cnpg}.yaml`, so they *are* build targets; the earlier "no build
    target" worry was wrong) + `github-actions-runner` (labeled in its own
    `platform/kubernetes/github-actions-runner/namespace.yaml` — applied by the platform kustomization,
    outside clusters/wind's build). Each verified non-violating via `kubectl label … --dry-run=server`
    (gha fails `restricted` — ARC needs caps — so baseline is its ceiling). All 3 live `enforce=baseline`.
  - **plex deliberately NOT enforced** — corrects this item's earlier inclusion of plex. plex keeps its
    existing `tier=system` "GPU passthrough needs privileged" carve-out (alongside wireguard). Its pod is
    baseline-clean *today*, but enforcing would block a future privileged transcode config the operator
    chose to preserve; marginal gain on a single media server isn't worth removing that option.
  - **7 stale/empty orphan namespaces DELETED** (`kopia`, `technitium-dns`, `rclone-gdrive`, `home`,
    `media`, `infra`, `gpu-operator`) — each had 0 pods / only the auto-created `default` SA, **no git
    source** (Flux won't recreate; `helm-releases/gpu-operator.yaml` manages `gpu-operator-system`, not
    the empty `gpu-operator`) and **no `targetNamespace` ref**; the live `dns`/`rclone`/`home-automation`
    services (the names they resemble) stayed healthy. User-authorized direct `kubectl delete`. DNS
    re-verified post-delete. `cilium-secrets`/`pg-recovery`/`ceph-csi`/`multus-system` are legit-empty
    system ns — left.

### ✅ M73. Admission policy engine (Kyverno) — BOTH GUARDRAILS ENFORCING 2026-06-28
- **✅ `require-resource-requests` → Enforce (2026-06-28, commit `dcd4f02`).** The audit-fail workloads
  were third-party Helm sidecars + dynamically-created pods (tailscale proxies, ARC ephemeral runners,
  ceph csi) that no chart edit reliably covers. Solved with **`02-add-default-resource-requests`** — a
  Kyverno **mutate** that injects a tiny default request (10m/32Mi) on any container missing one, BEFORE
  the validate webhook (only adds where absent; never overrides). **Verified safe across every path:**
  bare pod, multi-container sidecar (each gap filled), existing-requests preserved, and — the critical
  Flux/Renovate case — a request-less Deployment template is admitted with requests mutated in (Kyverno
  autogen covers controllers). No pods disrupted. Belt-and-suspenders with the policy-baseline LimitRange
  defaults in app namespaces. Existing 58 audit-report entries clear as controllers next reconcile.
- **✅ ENFORCING `disallow-latest-tag` (2026-06-27, commit `ba130f9`).** Audit review found **0
  violations** for this policy (cue-api excluded; all operators use tagged/pinned images so even
  dynamically-created pods pass), so flipped both its rules `failureAction: Audit→Enforce`. **Verified
  live:** a `:latest` pod in `default` is **denied** at admission, a tagged pod is allowed, and `:latest`
  in the excluded `cue` ns is allowed; 0 running pods disrupted (enforce doesn't evict; fail-open via
  `failurePolicy: Ignore`). **`require-resource-requests` STAYS Audit by design** — its 59 audit fails
  are ~6 third-party Helm charts (kube-prometheus-stack, traefik, tailscale-operator, ARC, ceph
  csi-rbdplugin in `default`, the s3-sync cronjob), several creating pods **dynamically** (tailscale
  proxies, ARC ephemeral runners, csi on reschedule) → enforcing would block those = outages. **Prereq
  to enforce it: add `resources.requests` to those charts' values (+ dynamic-pod templates), re-confirm
  `kubectl get polr -A` clean, then flip** (documented in `00-require-resource-requests.yaml` header).
- **Source:** zero-trust assessment 2026-06-17 (archived). No policy engine before now.
- ✅ **Engine deployed** (`clusters/wind/helm-releases/kyverno.yaml`, chart 3.8.x / Kyverno **v1.18.1**): 4 controllers healthy. Found a pre-existing `cluster-policy` that turned out to be the **NVIDIA gpu-operator's** `ClusterPolicy` (`nvidia.com`, not `kyverno.io`) — Kyverno itself has **zero** prior policies. Webhook is **fail-open** (`failurePolicy: Ignore`).
- ✅ **Starter policies (audit-only)** in `platform/kubernetes/kyverno/`: `require-resource-requests`, `disallow-latest-tag` (cue excluded — intentional `:latest`, H30/M64). Both Ready, `validate.failureAction: Audit`, control-plane + operator namespaces excluded. **Verified admission still works** (a `:latest`/no-requests pod admits under audit). Kyverno **v1.18 API** = per-rule `validate.failureAction` + `spec.webhookConfiguration.failurePolicy` (the spec-level fields are deprecated). Safety model + ops in the dir README.
- ⏳ **Next (promote + extend):** review `kubectl get polr -A` (populates on the background-scan interval / next admission), then flip clean rules `Audit→Enforce` per-rule. **Add image-signature/provenance** (cosign `verifyImages`) to back H30 — the higher-value provenance policy, deferred (needs the signing setup). Optionally a `require-digest` policy. **Effort:** S–M to promote + add cosign.

### ✅ M74. Cilium Tetragon — eBPF runtime security / detection (assume-breach) — DETECTION PIPELINE LIVE 2026-06-28
- **Source:** zero-trust assessment 2026-06-17. We have Cilium/Hubble (flow visibility) but no **runtime** detection. **Do:** deploy Tetragon (Helm), default observability policies on tier-1 namespaces, events → Loki/alertmanager + AI advisor. Spec in the assessment doc. **Effort:** M.
- **✅ v1 DEPLOYED observe-only (2026-06-24, `a4ba00f`).** Flux HelmRelease `tetragon` (chart 1.7.x from the `cilium` repo), ns `tetragon` `tier=system`. eBPF sensors loaded on all 8 nodes (DS 8/8, pods **1/1**). **No enforcement/kill.** Events on demand via gRPC: `kubectl exec -n tetragon ds/tetragon -c tetragon -- tetra getevents -o compact` — verified live. **stdout export OFF on purpose** (Alloy tails all pod logs → single-binary Loki; a Tetragon firehose would flood it). ⚠️ **Footgun caught same session:** the initial deploy used `export.stdout.enabled: false`, which is **not a real chart key** — Helm silently dropped it, leaving the chart default (`export.mode: "stdout"`) active = the `export-stdout` sidecar ran the full firehose (~800 lines/min cluster-wide, one node 497/min ≈ 1.1M/day into Loki). Fixed to **`export.mode: ""`** (`a4ba00f`); sidecar gone, pods now 1/1 (a second `export-stdout` container = export back ON). See `platform/kubernetes/tetragon/README.md`.
- **✅ v2 — DETECTION PIPELINE LIVE 2026-06-28 (commits `13e675d` part 1 + `5584529` part 2).** Shipped in two stages (policies first w/ export OFF to prove matches with zero Loki risk, then export+alert):
  - **(1) TracingPolicies** (`platform/kubernetes/tetragon/`, cluster-scoped, `monitor_only`): `detect-cred-file-access` (`security_file_permission`+`security_path_truncate` on `/etc/shadow`,`/etc/gshadow`,`/etc/sudoers*`,`*/.ssh/authorized_keys` — narrowed from upstream `filename_monitoring.yaml`'s broad `/etc/`) + `detect-setuid-root` (`sys_setuid` filtered `uid==0`). Both pass `--dry-run=server`, loaded `enabled` on all 8 agents.
  - **(2) Selective export** (HelmRelease values): `export.mode: stdout` + `tetragon.exportAllowList` restricted to `PROCESS_KPROBE/TRACEPOINT/UPROBE/LSM` (the `PROCESS_EXEC/EXIT` ~1.1M/day firehose is NOT allowlisted → never flows) + the chart-default namespace denylist (host-ns/system dropped — so runc's host-ns `setuid(0)` doesn't export). **Measured steady-state export = 0 lines/min.** ⚠️ DS pods are now **2/2** (tetragon+export-stdout) — CORRECT for v2 (volume safety = the allowlist, not 1/1).
  - **(3) Alert** (`monitoring/11-loki-rules-tetragon.yaml`): `TetragonCredFileAccess` (critical) + `TetragonSetuidRoot` (warning) off `{namespace="tetragon",container="export-stdout"}`, mirroring the hubble-audit ruler pattern. Rules-sidecar loaded `tetragon.yaml` into the ruler (confirmed).
  - **e2e PROVEN:** triggered `/etc/shadow` read in a pod ns → matched → exported → all alert JSON field-paths (`process_kprobe.{policy_name,function_name,process.binary,process.pod.*}`) resolve.
  - **✅ Follow-up 2026-06-28 (commit `c5e3100`):** added 2 more high-signal detections — `detect-ptrace-inject` (`sys_ptrace` PTRACE_ATTACH/SEIZE = process injection / live cred-dump) + `detect-pivot-root` (`sys_pivot_root` from non-init = container breakout) + their **critical** alerts (`TetragonPtraceInject`, `TetragonPivotRoot`). Both verified loaded `monitor_only` with **NPOST=0** (zero false-positives; host-ns runc filtered, self-trace + ns-pid 0/1 excluded); export still 0 lines/min.
  - **⏳ Still deferred (optional):** **shell-in-container** (a naive exec policy floods on liveness/exec-probes + entrypoints → degrades the pipeline; needs interactive/tty-only design since the exec firehose is export-denied), kernel-module-load / `sys_mount` (mount is CSI-noisy), and **enforcement mode** (`matchActions` kill) after the observe phase is trusted. Full detail: `platform/kubernetes/tetragon/README.md`.

### ✅ M75. In-cluster workload identity for cloud access (kill long-lived IAM secrets in etcd) — DONE 2026-06-24 (bar orphaned-key teardown)
- **Source:** zero-trust assessment 2026-06-17. velero/rclone/CNPG-barman/ai-advisor/cloudwatch auth to AWS with **long-lived static IAM keys in K8s Secrets** — same standing-credential class H29 killed in CI / M71 targets on the mini. (Diagnosis note: the ~13 AWS-cred secrets turned out to be **4 distinct dedicated per-workload keys**, not one shared key — `AKIA4C5DM33X` is just the account-ID prefix on every key in the account.) **Done:** self-hosted IRSA → pods assume per-workload least-priv roles for short-lived creds, **no static keys in etcd**. Extends [[H29]]/[[M71]]. Full design + procedure + gotchas: [`docs/runbooks/irsa-workload-identity.md`](../../runbooks/irsa-workload-identity.md). **Effort:** M–L.
  - **✅ Phase 1+2 DONE + internet-verified (2026-06-24, `6e3d382`/`347d210`):** new TF stack [`infra/terraform/aws/cluster-irsa/`](../../../infra/terraform/aws/cluster-irsa/) + `terraform-cluster-irsa.yml` CI workflow, applied via CI dispatch (CI's `gh-actions-terraform` role already permits OIDC-provider/role/policy creation — no admin bootstrap). Created: public OIDC discovery bucket `wind-cluster-oidc-830881980142` (issuer `https://wind-cluster-oidc-830881980142.s3.us-west-2.amazonaws.com`, serving `/.well-known/openid-configuration` + `/keys.json` = cluster public SA keys), IAM OIDC provider (live-computed thumbprint), + 4 least-priv roles trust-locked to exact ns/SA: `wind-irsa-{velero,s3-sync,barman,cloudwatch-read}`. Verified from the public internet: discovery `issuer` matches URL, `jwks_uri` resolves, JWKS == cluster keys. **Harmless until Phase 3+4.** (Hit + fixed an S3 `InvalidTag` — bucket tag values reject parens.)
  - **✅ Phase 3 DONE + e2e-verified (2026-06-24):** flipped kube-apiserver `--service-account-issuer` on all 3 CPs (cp2→cp3→cp1), then **collapsed to a SINGLE bucket issuer** once bound tokens refreshed. **Pinned `--api-audiences`** (CRUCIAL: it defaults to the first issuer → would 401 every token otherwise; per-CP-validated 403). Proven e2e: `velero-server` token → real `AssumeRoleWithWebIdentity` → short-lived `ASIA…` creds.
  - **✅ Phase 4 DONE (2026-06-24):** migrated ALL in-cluster AWS workloads to IRSA via **manual token projection (no webhook)** — velero (server+node-agent), the 7 s3-sync shares + approval-server + validation-job + daily-report + unifi-backup, CNPG postgres + cue-db (barman `inheritFromIAMRole` + `projectedVolumeTemplate`), ai-advisor, cloudwatch-to-loki. Each verified (velero+CNPG backups completed; STS-assume + S3-list for the rest; ai-advisor boto3 DescribeLogGroups). **Deleted all static-key secrets — no static AWS keys in etcd.** Gotchas fixed: velero double-env Helm failure; velero Kopia-maintenance needs `AWS_REGION` (else `sts..amazonaws.com`); aws-CLI non-root pods need `HOME=/tmp`. Commits `f7bb8c5`→`e327a82`.
  - **✅ Persistence VALIDATED (2026-06-24, `2668cbe`):** `kube_kubeadm_apiserver_extra_args` in the kubespray inventory (`kube_control_plane.yml`) carries the single bucket issuer + api-audiences; the **live manifests were collapsed to match it and re-verified in production** → live == IaC, zero drift, a future kubespray run reproduces working IRSA.
  - **⏳ ONLY follow-up — deactivate/remove 4 orphaned IAM keys:** ai-advisor-readonly + barman-postgres (TF-managed: ai-advisor-iam / s3 stacks) + velero + kubernetes-s3-backup (find their stack). Unused now but key material lingers in git history + Active in AWS → remove the `aws_iam_access_key` resources via their TF stacks + apply. **NOT the H29 `terraform-homelab` key.** SES SMTP secrets stay static (protocol can't use IRSA). etcd-backup is host-level (CP systemd), NOT IRSA → [[M71]].

### ✅ M77. Selective PVE VM firewalling — ALL 6 standalone VMs default-deny inbound 2026-06-28 (Stage 2b: asterisk source-scoped 2026-06-29; vpn-local narrowing optional)
- **🟡 STAGE 1 APPLIED + VERIFIED 2026-06-25 (`infra/terraform/proxmox/firewall/standalone-vms.tf` + NIC `firewall=true` in `standalone-vms/main.tf`).** Staged exactly like H37 (lock-out-safe): per-VM firewall **ENABLED but PERMISSIVE** (`input_policy = ACCEPT`, inbound logging ON) so nothing is denied while the allow-lists are confirmed against the real PVE firewall log — avoids the H37 Ceph/IPMI latent-break class. Allow-lists from live `ss -tlnp/-ulnp` (2026-06-25): a shared `vm-baseline` security group (SSH 22 from the `mgmt-admin` IPset + node_exporter 9100 from the Servers/K8s VLAN) + per-VM extras (dns-fallback 53 tcp/udp + 5380-from-mgmt; vpn-local WG 9820-9821; gh-runner baseline-only; asterisk 5060/5061 + RTP 10000-20000; devbox tailscale 100.64.0.0/10). `rpcbind:111` deliberately NOT allowed (eventual default-deny closes it). Applied via CI (firewall run `28196232091` → standalone-vms run `28196405267`; the NIC apply was user-authorized after a classifier gate); plans were exactly the change (11 add / 5 in-place NIC toggles, 0 destroy/reboot). **Verified post-apply:** all 5 VMs SSH-reachable (uptimes intact — no reboot), DNS resolves via dns-fallback `:53`, `:9100` scrape returns 200. **NEXT — STAGE 2 (per-VM, deliberate):** watch the PVE firewall log → scope external sources (Twilio SIP/media ranges for asterisk; AWS VPN peer IP for vpn-local; keep SSH+tailnet for devbox — the Claude session lives on it) → flip `local.vm_input_policy` ACCEPT→DROP per VM. gh-runner & dns-fallback are the safe first DROP candidates.
- **🟢 STAGE 2 STARTED + VERIFIED 2026-06-28 (commit `74dc87f`, firewall apply run `28331175739`).** Split `local.vm_input_policy` from a single global into a **per-VM map** and flipped the two safe first candidates **`dns-fallback` (1001) + `gh-runner` (1003) ACCEPT→DROP** (default-deny inbound). Chosen first because their allow-lists are **fully internal/baseline** (no external-source scoping needed) and were `ss`-verified at Stage 1; PVE's firewall is **stateful** so outbound-initiated traffic + replies are unaffected. **Plan was exactly the change** (`0 add, 2 change, 0 destroy`, in-place, no reboot). **Verified post-apply:** (a) **gh-runner self-test** — the apply *runs on* the gh-runner self-hosted runner, flipped it to DROP, and still completed + reported success → the runner survived its own flip (outbound-only, as designed); (b) **DNS** — `dig @10.10.201.6` resolves `auth.wind.etherport.net`→`10.10.201.70` (internal A) + forwards `github.com`, AAAA NODATA → `:53` tcp/udp serve fine under DROP. (NB: relied on the Stage-1 `ss`-enumeration + stateful-firewall reasoning + this functional verification in place of a fresh PVE-log read — headless `pve` shell access isn't in scope from the devbox.) **Remaining (4 VMs still ACCEPT):** `vpn-local` (scope AWS WG peer IP), `asterisk-sbc` (scope Twilio SIP/RTP ranges), `devbox` (Claude session host — keep SSH+tailnet, extra care), `step-ca` (confirm fleet+tailnet cert clients first).
- **✅ STAGE 2 COMPLETE 2026-06-28 (batch 2, commit `4bbf2ce`, apply run `28334106865`).** Flipped the remaining **4** — `step-ca` (1006), `devbox` (1005), `vpn-local` (1002), `asterisk-sbc` (1004) — ACCEPT→DROP. **All 6 standalone VMs are now default-deny inbound.** Existing per-VM port allows were KEPT (the flip only closes UNLISTED ports, e.g. `rpcbind:111`); plan was exactly `0 add, 4 change, 0 destroy`, in-place, no reboot. **Per-VM safety basis:** `step-ca` allows already source-scoped (Servers VLAN+tailnet :8443, mgmt SSH); `devbox` only extra listener was rpcbind:111 (no VNC) + access kept via SSH(mgmt-admin)+tailscale(devbox-initiated→conntrack) + reversible via CI (apply runs on gh-runner, not devbox); `vpn-local`/`asterisk` keep any-source on their service ports so WG/SIP/RTP are unaffected (stateful FW keeps live sessions). **Verified post-apply:** step-ca `:8443` `{"status":"ok"}` reachable under DROP, apiserver reachable from devbox (session intact), DNS via `.6` still resolves, `Apply complete 0/4/0`. **Stage 2b (external-SOURCE narrowing):** (1) **✅ asterisk SIP/RTP → Twilio/Talk ranges — APPLIED 2026-06-29 (commit `bf906f3`, apply run `28396740159`, `Apply complete 2 added/1 changed/0 destroyed`).** Added two IPsets + scoped the 4 asterisk rules to exactly the ranges the SBC's own `pjsip identify` ACL already trusts (so the firewall can only ever be a superset-or-equal of what the SBC would answer → cannot drop a call the SBC would accept): **5061/tcp (SIP-TLS, internet-facing Twilio leg) ← `twilio-signaling` IPset** (the 8 Twilio signaling /30s = `asterisk-sbc.yml` `twilio_signaling_nets`); **5060/udp (plain SIP, internal Talk leg) ← `asterisk-internal` IPset** (Talk `10.10.199.0/24` + Servers `10.10.201.0/24`); **10000:20000/udp (RTP media) ← Twilio media `168.86.128.0/18` (`twilio_media_net`) + `asterisk-internal`**. CI plan reviewed (only the asterisk resources, 0 destroy) before a user-authorized apply (classifier-gated as 911-critical). **⚠️ LIVE-CALL TEST REQUIRED to fully trust** (RTP has no app-layer backstop; can't test a call from the devbox) — user to place an inbound + outbound call (ideally a 911/provider-test) to confirm two-way audio; **revert = one-commit `git revert bf906f3` + re-apply** flips every rule back to any-source. (2) **vpn-local WG → AWS EIP `44.240.60.80/32`** — LOW value (WireGuard is crypto-authenticated, so IP-scoping only trims port-scan/log noise); do only if desired.
- **Source:** H37 follow-on 2026-06-17. Phase 2 of the Proxmox firewall (H37 = host mgmt plane). **k8s nodes (100-102,110-113,120) are explicitly EXCLUDED** — their traffic security is owned by Cilium (H3 NetPol + M66 WireGuard) + UDM zones/L3 ACLs (M56/M52); a PVE VM firewall there is redundant and risky (must hand-maintain 6443/etcd/kubelet/VXLAN/BGP/MetalLB allows; any gap breaks the cluster). **Standalone VMs** get per-VM default-deny-inbound: **gh-runner** (1003, high value — outbound-only runner → deny inbound except mgmt), **asterisk-sbc** (1004 — scope SIP/RTP inbound to Twilio ranges + LAN), **vpn-local** (1002 → WG udp/9820-9821 + mgmt), **dns-fallback** (1001 → :53 + mgmt), **devbox** (1005 → SSH/mgmt). All NICs currently `firewall=0`, so enabling is per-VM opt-in. Overlaps UDM north-south; value is defense-in-depth + intra-Trusted micro-seg (the broad `Trusted→Trusted` allow from M56). Needs per-VM service mapping. **Effort:** M.

### ✅ M76. SSH to nodes/VMs via short-lived certs — step-ca hybrid — **CUTOVER DONE 2026-06-26**
- **✅ CUTOVER COMPLETE 2026-06-26 (cont.): the running fleet is CERT-ONLY.** Last 2 consumers switched first — the **2 CONTAINER** ansible workflows (vm-fleet, proxmox; `setup-ssh-cert` self-installs the pinned step CLI into the container, commit `e9e28e3`, proxmox check-mode smoke-tested green). Then **removed `automation@homelab` from `authorized_keys` on all 15 hosts** (`step-ca-remove-static-key.yml`, surgical `state=absent`, run `--private-key ~/.ssh/id_homelab_cert` so it can't cut its own connection) — verified BEFORE: 15/15 cert-reachable (step-ca first added to user-CA trust so it wasn't stranded); verified AFTER: cert still 15/15 **and static key REJECTED** (k8s-w1/pve/step-ca). `pve-sshd.yml` stops re-asserting it (root@pve cert-only; graham-mac break-glass + unifi-cert-sync kept). **Devbox** is now cert-only too (`devbox.yml` no longer deploys the key, ssh-config single `IdentityFile`; live key file removed; SSH verified via config alone). Commits `3e811d9` (cutover) + `3b6f994` (devbox + bootstrap annotations). **Residuals BY DESIGN (documented, not standing fleet access):** (1) **cloud-init** keeps the key as the per-host **BOOTSTRAP** pubkey (new host must be reachable to enroll → then `step-ca-remove-static-key.yml` strips it) — the 3 TF vars are annotated; (2) **packer** keeps the static key (build-VM access); (3) **appliances** keep scoped legacy keys (hard carve-out). **Residual HOLDERS to clean (key material still exists, but grants nothing on the running fleet):** the **mini** copy (→ rolls into [[M71]]) + the GH **`ANSIBLE_SSH_KEY`** secret (now bootstrap-only; user removes via UI — the M92 PAT lacks Secrets scope) + the SOPS `automation_ssh_private_key` (kept as the re-add/break-glass source). **Break-glass = PVE console + IPMI 10.10.200.21.** Honest caveat: this is the *intended partial* win — traded the standing fleet key for a CA crown-jewel + a standing JWK-provisioner password; bootstrap key persists for new-host enrollment.
- **✅ 2026-06-26 cont.: SSO + host certs + BOTH headless consumers now on certs.** **SSO** (`step ssh login` via Authentik, gated to the Admins group, SSH template grants `ubuntu`/`root`) works — the operator's Mac authenticates with the cert (config: homelab `Host` blocks above `Host *`, `IdentityAgent SSH_AUTH_SOCK`; 1P retained for non-homelab). Dropped the OIDC `--domain` filter (it 401'd gmail identities). **Phase 2b host certs** (`step-ca-hostcerts.yml`, commit `19ef435`): all 13 Servers-VLAN hosts present a host cert → known_hosts TOFU killed (pve excluded — mgmt VLAN can't reach the CA). **CI consumer-switch** (commit `6462f0d`): `setup-ssh-cert` composite action mints a ≤1h cert from the **`STEP_JWK_PASSWORD`** repo secret; the **4 host-context** ansible workflows (k8s-node-fixes, kubespray, inventory-drift, post-bootstrap) switched + inventory-drift smoke-tested green. **devbox consumer-switch**: `infra/devbox/step-ssh-renew.{sh,service,timer}` + `devbox.yml` ssh-config → the **agent now authenticates with the cert** (verified `id_homelab_cert ECDSA-CERT`). **REMAINING for key removal:** (1) the **2 CONTAINER** workflows (vm-fleet, proxmox — `step` isn't in the container; need step-in-container or de-container), (2) **packer** stays on the static key by design (build-VM access), (3) then remove `automation@homelab` from authorized_keys + the 4 deploy points + 3 holders. Static key still live (safety net).
- **✅ PHASES 1–4 DONE 2026-06-26 → [`m76-ssh-shortlived-plan.md`](m76-ssh-shortlived-plan.md). Cert-SSH is LIVE fleet-wide (additive).** step-ca `active` on VM 1006 (`https://10.10.201.46:8443`), all 4 provisioners (`admin`/`sshpop`/`headless`(JWK)/`authentik`(OIDC)), fingerprint `a37b7b16…89cf67`, DNS resolves. **Phase 2–4 (commit `51400c7`):** `step-ca-trust.yml` pushed `TrustedUserCAKeys` (step-ca user CA pubkey, sshd drop-in `60-step-ca.conf`) to **all 14 fleet hosts** (8 k8s nodes + 5 standalone VMs + pve), ADDITIVE (sshd `-t`-validated before reload; the `automation@homelab` key is untouched + still works in parallel). **Proven e2e:** the devbox (step-cli bootstrapped) mints a 10-min cert via the JWK `headless` provisioner and SSHes **cert-only** (`-F /dev/null`, no static key) to k8s-w1/cp1, dns-fallback, gh-runner → CERT-AUTH OK. CA pubkeys in `files/step-ca/`. **OIDC fix:** persistent netplan route `.70/32 via .1` (VLAN-201 can't ARP the BGP VIP — memory `vlan201-host-cant-reach-metallb-vip`). **NEXT = Phase 5 CUTOVER (the risky step — do deliberately):** switch the headless consumers to certs first (the **CI ansible**/`ANSIBLE_SSH_KEY` → mint a JWK cert in-workflow; the **devbox agent** ssh-config/renew-loop), verify cert-only works for every flow, THEN remove `automation@homelab` from authorized_keys + the **4 hardcoded deploy points** (TF/Packer/ansible/AWS cloud-init) + the **3 holders** (mini, devbox, GH secret). Break-glass = PVE console + IPMI. Appliances keep scoped legacy keys (hard carve-out). **Follow-ups:** persist step-cli install+bootstrap in `devbox.yml`; optional Phase-2b host-certs (sshd `HostCertificate` + client `@cert-authority`) to kill known_hosts TOFU. **Decision recap:** chose **step-ca SSH CA (hybrid)** over Tailscale-SSH-for-fleet. **Why:** the two heavy consumers are HEADLESS (the Claude devbox agent + CI ansible) — the live Tailscale ACL is `action: check` (interactive, breaks headless) and the **fleet is NOT on the tailnet** (only devbox + the offline vpn-local subnet router are); and the **LAN/no-TS path** is the decider — Tailscale SSH only governs the `100.x` tailnet transport, so LAN-IP SSH bypasses it, whereas **step-ca certs are transport-agnostic** (`TrustedUserCAKeys` → work on LAN IP, tailnet, anything; ansible-friendly). **Hybrid:** step-ca primary for the **13 Ubuntu hosts** (8 k8s nodes + 5 standalone VMs + PVE; humans via the existing **Authentik** OIDC, headless via a JWK renew-loop), Tailscale-SSH `check` retained for interactive remote, **PVE console + IPMI `10.10.200.21` = break-glass** (run step-ca OFF-cluster to avoid the circular dep; set console break-glass passwords since cloud-init users are key-only). **Appliances are a hard carve-out** (UDM/UNAS/Protect/PDU/UPS — vendor firmware, keep scoped legacy keys). Honest caveat: it's a *partial* win — trades the standing key for a CA crown-jewel + a standing JWK-provisioner password. The static `automation@homelab` key currently sits on **3 disks** (mini, devbox, GH secret `ANSIBLE_SSH_KEY`) + **4 hardcoded pubkey deploy points** (TF/Packer/ansible/AWS cloud-init) — all removed only at Phase-5 cutover, AFTER the cert path is proven e2e on one host with non-interactive headless renewal. 6-phase plan in the doc.
- **Source:** zero-trust assessment 2026-06-17. Pairs with **M71** under one "kill standing creds" theme (M75 did the in-cluster IRSA leg). **Effort:** M.

### ✅ M78. OneDrive (work) backup → NAS → S3 — superseded by [[M88]] (done 2026-06-20)  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ M80. iCloud Drive + Contacts + Messages + Notes/Safari backup → NAS → S3 — DONE 2026-06-23
- **Source:** owner 2026-06-17 (lower priority than Photos). **Drive:** rclone `iclouddrive` backend (app-specific pw) OR the mini's native CloudDocs sync (`~/Library/Mobile Documents/com~apple~CloudDocs/`) → rsync — both yield individual files. **Contacts:** **`vdirsyncer`** over iCloud **CardDAV** (app-specific pw) → individual `.vcf` vCards (clean, no sign-in). **Messages:** back up `~/Library/Messages/` (`chat.db` + attachments) — requires the mini signed into iMessage; it's a SQLite DB + attachment files (restorable to a Mac), the only viable path (no API). All → NAS `Backups` share → S3. **Effort:** M.
- ✅ **DONE (mini agent, 2026-06-22): Contacts + Calendars LIVE + scheduled.** 2,049 contacts + 446 calendar events → `/Backups/Graham/iCloud/{Contacts,Calendars}` (rides 01:00 `s3-sync-backups` → S3). rc=0/items/`last_success` push green to Pushgateway (`contacts_backup_*`/`calendars_backup_*`). LaunchAgent `net.wind.icloud-dav` loaded — **daily 21:00** (before 01:00 S3 → ships same night). Incremental run **11s**. **Design:** vdirsyncer pulls iCloud → LOCAL staging (`~/.local/share/icloud-dav/`) then `rsync -a --delete` → NAS (direct-to-SMB stalls the iCloud pull; small-file metadata latency ~100 files/s). iCloud storages `read_only=true`. Two wrapper bugs fixed (`9e04e9f`): vdirsyncer-config inline-`#` comment broke INI parse; `pipefail`+`yes|discover` SIGPIPE(141) skipped the sync. **Cluster-side observability shipped:** templated **"Mac mini — iCloud backups"** Grafana dashboard (`dashboards/icloud-backups.yaml`) — a `cat` variable = `label_values({__name__=~".+_backup_last_rc"}, job)` drives a per-category row that REPEATS, so messages/drive appear automatically. Alerts authored (`10-icloud-backups-alerts.yaml`: `ICloudBackup{Stale,Failed,Empty}`) — **NOW SAFE TO UNCOMMENT** in kustomization (syncs are green). Commit `9a45f30`.
- ✅ **Messages tier DONE (2026-06-23):** `messages-backup.sh` (20:00) → `/Backups/Graham/iCloud/Messages/`. chat.db **319,679 msgs** (integrity ok) + **19,951 attachment files / ~48 GB**. Attachments use a **parallel sharded** mirror (256 hex top-dirs × 6-way `xargs -P` rsync — serial was ~1/s/~5h; parallel ~6×). Split metrics: `messages_backup` (DB) + `messages_attachments_backup` (files). FDA: must be on **`/bin/bash`** (responsible process), not rsync. Bugs fixed: xargs-stdin-via-mini_run_timeout false-success; launchd net-vol FDA probe.
- ✅ **Notes + Safari + iCloud Drive DONE (2026-06-23):** `icloud-files-backup.sh` + `net.wind.icloud-files.plist` (19:30). Notes (group container; NoteStore.sqlite integrity-checked) **414 notes/397MB**; Safari `Bookmarks.plist`; iCloud Drive (CloudDocs — only 2 local files, owner doesn't use it). Metrics `notes_backup`/`safari_backup`/`icloud_drive_backup`. **Reminders already covered** (CalDAV VTODO in Calendars). **M80 iCloud coverage COMPLETE** for everything in use.
- ✅ **mini→Traefik-VIP observability outage RESOLVED 2026-06-23 (cont.5, `1a98eee`).** Root cause was the traefik-tier netpol allowing SERVICE not CONTAINER ports (see H3 / CLAUDE.md §5) — NOT a backup problem. Mini metric pushes + log shipping resume on the next cron; iCloud-backup dashboard + `ICloudBackup{Stale,Failed,Empty}` alerts are wired (`10-icloud-backups-alerts.yaml`). Only residual: a one-time operator `sudo` to bootstrap the `net.wind.nsmb-install` LaunchDaemon (SMB tuning) per `infra/macos/mini/README.md`.
- ✅ **Adversarial review of all M79/M80 code (2026-06-22):** 4 CRITICAL + HIGH found+fixed (`ceb5027` rsync-wipe guard + metric-masking; `adbade8` `mini-common.sh` lock/timeout/kill-tree hardening; `4697f23` nsmb→/etc; `3103ac1` PhotosExportNotParsed alert). See session-log. **Residuals:** (a) ⏳ **operator one-time sudo** to bootstrap the `net.wind.nsmb-install` LaunchDaemon (commands in `infra/macos/mini/README.md` → SMB tuning) — until then SMB tuning isn't applied to kernel mounts; (b) ⏳ infra-agent: confirm Prometheus scrapes pushgateway + the new `-1`/`summary_parsed` sentinels don't mis-trip alerts; (c) ⏳ MEDIUM `count_orphans` basename-collision undercount + silent `-1` guard-disable (photos-metrics.sh H5).
- 🟡 **Photos pipeline observability also extended (2026-06-22):** split `photos_export_missing` → `_resolvable` (actionable) / `_unavailable` (edited Live-Photo clips, expected ~9) + Coverage stat + reframed `PhotosExportCoverageRegressed`; added `photos_export_orphans` "Orphan dup files" stat + `PhotosExportOrphansGrowing` (growth-only) alert. **Orphan dedup run #2 executed:** 1,058 ledger-orphan dupes deleted from NAS (15.77 GiB) + S3 versions purged (governance-bypass via a temp, prefix-scoped lift of the M97 Deny, applied+reverted via iam-apply OIDC — see memory `s3-backup-version-purge-needs-m97-deny-exception`); NAS=S3=43,837. (M96 was run #1.)

### ✅ M81. Migrate Claude Code dev sessions mini → devbox (reboot-durable) — DONE 2026-06-18  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

## LOW — completed items

### ✅ L24. Authenticate BGP sessions (MetalLB ↔ UDM) — DONE 2026-06-28 (FRR migration + TCP-MD5)
- **✅ DONE 2026-06-28 — MetalLB migrated native→FRR + TCP-MD5 live, verified both ends.** Executed the 4-phase plan in one window (the user at the UDM): **Phase 1** re-added a temporary L2Advertisement safety net (VIPs ARP-reachable as a cushion); **Phase 2** migrated MetalLB **off the kubespray native addon onto the Helm chart in FRR mode** (`clusters/wind/helm-releases/metallb.yaml`, chart 0.14.9, `speaker.frr.enabled=true`, `crds: Skip`) — deleted the native workloads + Helm-installed the FRR controller/speaker (frr/reloader/frr-metrics sidecars), `metallb_enabled: false` in kubespray; **the flap was SEAMLESS** (VIP never dropped — L2 net + pre-pulled `frr:9.1.0`); **Phase 3** set **TCP-MD5** (`spec.passwordSecret: bgp-md5`, SOPS `02-bgp-md5-secret.sops.yaml`) cluster-side + the matching `neighbor metallb password …` on the UDM peer-group — **MD5 proven** by "Connections established 2; dropped 1" (session dropped while one-sided, re-Established once both ends matched — rules out the FRR #6921 one-sided false-positive); **Phase 4** removed the temp L2 net (back to BGP-only → cleared the M36 IP-conflict churn) + added a **PodMonitor** (`metallb_bgp_session_up` from the frr-metrics `:7473` sidecar) + **PrometheusRule** `MetallbBGPAllSessionsDown`(critical)/`MetallbBGPSessionDown`(warning) closing the silent-withdrawal gap. **Verified:** 8/8 sessions Established w/ MD5 (UDM screenshot: 5 routes each), `sum(metallb_bgp_session_up)=8`, Technitium/.5 functional. Commits `4731097`(P1)→`67773a7`(MD5)→`cb63ab0`(P4 L2-remove)→`b2086ef`(alert). **FRR upsides:** ✅ **BGP graceful-restart is ALREADY ACTIVE** (FRR-mode default — verified `show bgp neighbor 10.10.201.1`: "Long-lived Graceful Restart: advertised and received", 120s restart timer; the UDM advertises+receives it too). ⏳ **Still optional/not-done:** **BFD** (BFDProfile + UDM BFD) for sub-second failover; and a **VLAN-201→VIP `/32 via .1` route** so TS/WG (which egress via VLAN-201 hosts) can reach the BGP VIPs (`.5` etc.) — see [[vlan201-host-cant-reach-metallb-vip]]. **🔎 Adversarial review (2026-06-28) follow-ups landed:** deleted the orphan `wind-l2`/`wind-pool` L2Advertisement + native ClusterRoleBindings; pin/Renovate-track the floating `0.14.x` chart (low). **⏳ Stage-2b/cleanup remaining:** ✅ asterisk SIP/RTP → Twilio source-scoping DONE 2026-06-29 (`bf906f3` — see M77 Stage-2b; live call test pending); the 3 "no GR yet" doc corrections (metallb.yaml header, metallb README, advisor-prompt-configmap) landed in the cont.8 review batch.
- **📋 SCOPED 2026-06-25 → [`l24-metallb-frr-migration-plan.md`](l24-metallb-frr-migration-plan.md). DECIDED: path A (FRR migration + MD5).** ⚠️ **The tracker's "Effort: S, set a password both ends" is WRONG.** MetalLB runs in **NATIVE BGP mode** (v0.14.8, gobgp) which **cannot do TCP-MD5** — the `password`/`passwordSecret` fields are honored **only in FRR/frr-k8s mode** (MetalLB #1125; Go 1.24 MPTCP lacks `TCP_MD5SIG`). And MetalLB is the **kubespray addon** (`metallb_enabled`), whose role has **no FRR toggle** → FRR mode means **migrating MetalLB off kubespray onto the official Helm chart** (Flux-managed, like Cilium). So L24 = a backend migration, realistically **MED/M, not LOW/S**. Upside: FRR mode unlocks **graceful-restart + BFD** (fixes the VIP-blackhole-on-speaker-restart risk that exists today) — frame it as a resilience upgrade, MD5 is the trivial follow-on. **Blast radius:** the single eBGP session (64513↔64512 @ 10.10.201.1) carries EVERY VIP incl. **Technitium DNS (.5/.71/.72)** + **Traefik (.70)** with no GR/BFD/L2 → a flap blackholes ingress+DNS together for ≤180s, **silently** (CiliumTraefikIngressDrop can't fire). Plan mitigates with a temporary L2Advertisement safety net + 2 maintenance windows + tcpdump-verify-MD5 (FRR #6921) + a new `metallb_bgp_session_up` alert. UDM side is **UI-only/no-API** (manual FRR edit; source-of-truth in [`../runbooks/archive/bgp-phase-c-udm-metallb.md`](../../runbooks/archive/bgp-phase-c-udm-metallb.md)).
- **Source:** zero-trust assessment 2026-06-17. Threat = rogue host on VLAN 201 injecting routes; "low real-world risk on a trusted fabric" per the assessment. **Effort:** ~~S~~ **M** (corrected).

### ✅ L15. Ship etcd snapshot timer logs to Loki — SUPERSEDED by M62 (2026-06-17)  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ L17. `setup-terminal.sh` hardening (2026-06-10)  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ L19. Verify Telegram webhook auth (cross-repo) — OBSOLETE (2026-06-24)
- The Telegram surface is **retired** (no `/telegram/webhook` route is registered in the `cue` app; the `TELEGRAM_*` secrets have been removed from `cue-app`). The public surface is now `cue.etherport.net` behind CF Access (per-path SSO / service token / `/health` bypass). No webhook to verify.

### ✅ L21. `terraform-google` auth → GCP WIF — RESOLVED 2026-06-13  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ L22. CI check that every `*.sops.yaml` decrypts (catch MAC/hand-edits) — DONE 2026-06-17  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_

### ✅ L23. Clean up orphan `cnpg-manager` ServiceAccount/RBAC (post duplicate-operator removal) — DONE 2026-06-17  — _full detail in [archive/completed-2026-H1.md](completed-2026-H1.md)_


---

## Extracted 2026-07-03 (Opus 4.8 full-repo sweep)

### ✅ H42. step-ca (+ asterisk-sbc, pve OS) monitoring blind spot — DONE 2026-07-01
- **✅ Fixed (`6c458f6`), e2e-verified:** node_exporter deployed to step-ca + asterisk-sbc (base.yml CI apply — never ran there) and to the pve HOST via the NEW standalone `node-exporter.yml` (full base.yml is a hypervisor hazard: its unattended-upgrades Automatic-Reboot would reboot every VM). NEW `pve-nodeexp` firewall group (:9100 from Servers VLAN) — **the PVE host firewall now has FOUR required allows** (CLAUDE.md updated). 3 new scrape targets + relabels; blackbox Probe on step-ca `:8443/health`; `StepCADown` critical alert (blast-radius + break-glass in the annotation). Verified: all 3 targets `up=1`, `probe_success{step-ca}=1`.
- **Source:** 2026-07-01 Fable-5 infra review (#1). `base.yml` installs node_exporter on step-ca/asterisk-sbc/pve, but `01-external-scrape-config.yaml` scrapes neither; **0 of 82 alert rules mention step-ca**; no blackbox probe on `https://10.10.201.46:8443`. Since M76 the whole fleet is cert-only SSH — a wedged step-ca silently breaks cert renewal and the devbox/CI lose fleet access at the next ≤13h expiry, unpaged (break-glass = PVE console/IPMI, but you'd find out the hard way).
- **Fix:** add the 3 targets to `external-nodes`, a blackbox probe on the step-ca endpoint, and a `StepCADown` (+ cert-renew-staleness) alert. **Effort: S.**


### ✅ H43. 9 TF workflows ran PR-authored `terraform plan` on the in-network self-hosted runner — DONE 2026-07-01
- **✅ Fixed (`72cb120`):** removed the `pull_request` trigger from all 9 self-hosted-runner workflows (cloudflare, google, twilio, twilio-webhook, unifi, proxmox ×4). Plans still run on push-to-main (the 5 stacks that had it), workflow_dispatch, and the daily drift-detection sweep (all 9). Renovate PRs no longer execute code on the lifecycle runner.
- **Source:** 2026-07-01 Fable-5 infra review (#2). `terraform-{google,twilio,twilio-webhook,cloudflare,proxmox-firewall,proxmox-sdn,proxmox-standalone-vms,proxmox-k8s-vms,unifi}.yml` trigger on `pull_request` and run on `[self-hosted, lifecycle]`. `plan` executes PR-authored code (`data "external"`, module/provider fetch) = code-exec on a persistent in-homelab runner whose env scope holds PVE/CF/UDM secrets — PR-reachable, worse than the accepted dispatch-only L20 posture. Renovate PRs traverse this path constantly.
- **Fix:** move PR plans to `ubuntu-latest` where the stack allows (regional-vpn/dns-restrict-ip already do), or drop the `pull_request` trigger on self-hosted stacks (keep push+dispatch). **Effort: M.**


### ✅ H44. Authentik upgrade 2024.12.3 → **2026.5.3 (latest) — DONE 2026-07-02; CVE-2026-25227 remediated + 7 majors of currency**
- **✅ Hops 1–4 DONE 2026-07-02 (CVE FIXED):** laddered 2024.12.3 → 2025.2.4 → 2025.4.4 → 2025.6.4 → **2025.8.6** (sequential-major per authentik policy). **CVE-2026-25227 (Critical RCE) is remediated at 2025.8.6** (fixed builds: 2025.8.6/2025.10.4/2025.12.4). Each hop: bump server+worker (embedded outpost auto-matches), Flux reconcile, migrations applied under the PG advisory lock, both replicas + worker Ready, 0 restarts. Pre-flight hardening: **startupProbes** added to server+worker (60×10s) so a long migration isn't liveness-killed mid-run (upstream #14501). Rollback anchor = CNPG backup `pre-authentik-upgrade-h44` (2026-07-02T13:51Z) + continuous WAL PITR. The 2025.8.6 `tenant.flags` ProgrammingError was a transient migration-ordering artifact (0 recurrence, health/ready=200).
- **✅ Hops 5–8 DONE 2026-07-02 (operator present):** 2025.10.4 → 2025.12.6 → 2026.2.4 → **2026.5.3** (latest). **hop 5 (2025.10):** redis→PG state migration clean (`django_postgres_cache`/`django_channels_postgres`); **Redis fully removed** (env + `20-redis.yaml` Deployment deleted — sessions had moved to PG at 2025.4, tasks at 2025.8). **hop 6 (2025.12) RBAC overhaul: `authentik_core.0056_user_roles` applied CLEANLY** — the flagged danger — because the group precondition was verified first (`ak shell`: **2 groups, 0 duplicate names**) + blueprints clean + startupProbe + fresh backup `pre-authentik-hop6-rbac`. All hops 0 restarts, migrations under advisory lock. **⚠️ OPERATOR TODO:** verify OIDC logins (Grafana/wiki.js/Open WebUI) + forward-auth admin UIs — 2025.10 flipped the `email_verified` claim default true→false; if any RP gates on verified email, add a custom scope mapping (not yet verified — agent can't browser-test). **The Redis 7.4→8 concern is MOOT** (redis is gone).


### ✅ H45. CVE patch batch — ALL DONE 2026-07-02: containerd 2.2.5, Cilium 1.18.11, CNPG operator 1.30.0, PG 16.14
- **✅ H45a Cilium DONE 2026-07-02 (`a5784c7`):** 1.18.6→**1.18.11** (CVE-2026-49445) via `helm upgrade` from the devbox (helm v3.19 installed → CLAUDE.md §4). ⚠️ `--reuse-values` threw a hubble.relay.logOptions nil-pointer template error → used `--reset-then-reuse-values`, which **re-enabled `policyAuditMode=true`** (helm values had drifted from the live ConfigMap hand-patches) — would have SILENTLY UN-ENFORCED all 6 netpol tiers. Caught + fixed with `--set policyAuditMode=false` + `rollout restart ds/cilium`. Verified: enforce mode, WG encryption, BGP 8/8, 0 drops. `cilium_version: 1.18.11` in kubespray inventory + live-values snapshot `docs/reference/snapshots/cilium-helm-values.yaml`.
- **✅ H45c CNPG OPERATOR DONE 2026-07-02:** laddered 1.24.1→1.25.1→1.26.1→1.27.1→1.28.1→1.29.1→**1.30.0** (sequential-minor per CNPG's documented no-skip policy; chart 0.22.1→0.29.0). Each hop verified operator-image + both clusters healthy + `ContinuousArchiving=True`. The **Critical 9.4 (fixed ≥1.28.3) is resolved.** cue-db single-instance restarts briefly per hop (multi-attach detach-lag ~4-6 min, self-clears); postgres-cluster HA-rolls with no downtime. Commits `e7e369f` + siblings.
- **✅ H45d PG data-plane bump 16.4→16.14 — DONE 2026-07-02:** VERIFIED the standard `cloudnative-pg/postgresql:16.14` still bundles pgvector (+ PGAudit + Failover Slots) → the cue-db `vector` extension survives. Bumped **postgres-cluster** (HA rolling switchover, no downtime; authentik survived, 0 restarts) then **cue-db** (single-instance restart). Pre-bump CNPG backups `pre-pg16-14-bump-h45d` (both namespaces) + WAL PITR as rollback. Both clusters healthy on 16.14, archiving intact.
- **✅ H45b containerd 2.2.5 + M123-patch K8s 1.34.3 — DONE 2026-07-02 (combined rolling window):** `kubespray.sh upgrade-cluster.yml` from the devbox (fresh venv; cert-SSH via `KUBESPRAY_SSH_KEY`; detached **tmux** after the harness killed a backgrounded attempt mid-download). All 8 nodes v1.34.3 + containerd 2.2.5; RECAP 0 failed/0 unreachable. Landmine checks ALL passed post-run: issuer/api-audiences intact (inventory-persisted → no IRSA break, no multus restart needed), multus 8/8, cilium ENFORCE+WG+BGP 8/8, `/opt/cni/bin` root:root (wrapper pre-flight). Gates first: etcd snapshot (`/var/backups/etcd-pre-h45b-m123-20260702.db` on cp1) + quorum 3/3 + velero `pre-upgrade-h45b-m123` (PartiallyFailed 1 error — it raced the drains; benign, etcd+CNPG were the real anchors). **Gotchas recorded in the runbook:** kubespray v2.30 checksum DICTS live in role vars/ (beat inventory — override the `containerd_archive_checksum` SCALAR); cue-db PDB stalled w3's drain exactly as predicted → pre-armed watch deleted it at +100s, CNPG rescheduled to w1. Runbook current-versions updated.


### ✅ M122. Close the update-automation blind spots (the systemic fix) — DONE 2026-07-02 (`122caaa`)
- Enabled renovate's **`flux` manager** on `clusters/wind/helm-releases/**` (was watching only gotk-components → the `version:` fields were invisible to renovate); patch+minor grouped weekly, majors as individual hand-review PRs (`prPriority:-1`). **Exact-pinned all 17 HelmReleases** to their deployed versions (the ranges silently FROZE majors — `alloy` sat on a `0.x` cap that 1.x never matched, so it was ~10 chart minors behind; kps `80.x`, cnpg on an EOL line, etc.). Upgrades now happen by merging PRs, never by silent drift/freeze.
- **CORRECTION (charter C3) vs the 2026-07-02 currency review:** the Grafana chart repo is **NOT dead** — the original `grafana.github.io/helm-charts` still serves alloy 1.10.0 / loki 7.0.0; the `grafana-community` repo carries a **diverged** loki 18.x lineage. The alloy staleness root cause was the **range cap**, not the repo URL. URL unchanged. **Remaining (⏳ L):** raw-manifest image coverage (authentik/redis/ceph-csi sidecars) not yet wired into renovate.


### ✅ L32. vpn-fallback (ex vpn-local) Tailscale — REINSTALLED + REJOINED 2026-07-02 (100.97.20.113, tagged-devices, exit-node advertised; owner to approve exit node + disable key expiry)
- **Found 2026-07-02** (owner tidy-up). `tailscale status`: `vpn-local` (100.73.247.54) **offline, last seen 47d ago**. Investigated: the **VM is UP** (28d uptime, SSH-able) but `systemctl is-active tailscaled` = **inactive** and it didn't come back on the last boot (tailscaled not enabled / failed). **Answer to "should TS have a non-swapping IP like WG?":** it already does — a node's `100.x` CGNAT IP is **stable/persistent** (unlike the WG floating VIP), so Tailscale IS the stable-address path; vpn-local just needs its daemon back. **UPDATE 2026-07-02:** it's bigger than a restart — Tailscale is **fully uninstalled** on vpn-local (`tailscaled.service` absent, `tailscale` binary gone), so it was removed at some point (rebuild? the M77 firewall/cleanup work?), not just stopped. **Fix:** run the `tailscale.yml` playbook against `inventory=wind --limit vpn-local` via the ansible-vm-fleet workflow (needs a TS auth key) to reinstall + join, then **disable key expiry** + tag `tagged-devices` in the admin console so it stays ACL-stable and doesn't drop again. Re-auth (`tailscale up`) needs an auth key/interactive URL → not fully headless. **Effort: S-M.**


### ✅ M128. Rename `vpn-local` → `vpn-fallback` — COMPLETE 2026-07-02 (repo+TF+PVE+guest hostname+TS rejoin as vpn-fallback)
- **Owner ask 2026-07-02.** Scope: **44 files** + a PVE VM rename + a TS-console node rename. NOT a safe blanket `sed` — it's a functional identifier with state/label ripple. Careful multi-part plan: (1) **TF** `infra/terraform/proxmox/{standalone-vms,firewall}/` — the VM `name` (in-place update) + the firewall resource addresses `*.vpn_local` → `*.vpn_fallback` need **`moved {}` blocks** (a bare rename = destroy+recreate the firewall rules = a brief firewall gap on VM 1002) + unifi reservation; apply via CI (user-gated). (2) **PVE VM name** 1002 → vpn-fallback (rides the TF `name` change or `qm set`). (3) **ansible** inventory host + `git mv host_vars/vpn-local.yml` + playbook refs (base/swap/tailscale/wireguard). (4) **monitoring** — the `01-external-scrape-config.yaml` relabel maps the IP to `instance="vpn-local"` → changing it renames the metric `instance` label (alerts `instance=~"vpn-.*"` still match; dashboards + `02-external-alerts` refs). (5) **auto-remediation** SSH host map + `advisor-ssh-key.sops.yaml` key ref. (6) **wireguard** manifests/keepalived/README. (7) **docs**. (8) **TS admin console** node rename. **Interplays with L33** (the WG-failover review may change vpn-local's role — sequence after/with it). **Effort: M (careful; TF moved-blocks + user-gated apply).**
- **✅ EXECUTED 2026-07-02 (all but TS):** repo layer (40+ files, commit `414e2a7`) with the exact moved{} strategy above — plans verified as EXACTLY the intended diff (standalone-vms: 1 in-place name change; firewall: pure moves 0/0/0; unifi: 1 in-place) → all 3 applies success. Guest hostname + /etc/hosts changed on the VM. The two `platform/wireguard/*vpn-local*.sops.yaml` files tolerated FILENAME rename only (comment edits broke their MAC — reverted; advisor-ssh-key.sops.yaml tolerated it fine). **Remaining:** TS re-join as vpn-fallback (owner deleted the old node from the console; needs an auth key → L32 reinstall), + the monitoring `instance` label flips on the next scrape-config reload (dashboards/alerts use `vpn-.*` matchers, unaffected).


### ✅ M123. K8s platform upgrade train — COMPLETE 2026-07-02: 1.34.3 (with H45b) AND the 1.35.0 minor, same day
- **1.35.0 minor (kubespray v2.31.0):** deprecated-API audit clean → submodule bump + venv (ansible 11) → rolling upgrade-cluster in tmux. All 8 nodes **v1.35.0 + containerd 2.2.5**; landmines all green (cni root, issuer/audiences intact, multus 8/8, cilium ENFORCE+WG+BGP 8/8, 0 non-running pods). Next train: 1.36 when kubespray ships it (quarterly cadence).
- **Two NEW run gotchas (recorded in kubernetes-upgrade.md):** (1) **gpu1's drain failed 3× because cue-db-1 had rescheduled onto the LAST un-upgraded node mid-rollout** — the drain watch must follow the pod, not its starting node (cordon-first + delete, then retry). (2) **Never include already-upgraded CPs in a `--limit` retry**: kubespray re-runs `kubeadm upgrade apply` which fails its post-upgrade phase on an already-upgraded node ("no flags found in kubelet env file") AND leaves the CP cordoned. Retry workers with `--limit <node>` ALONE (drain delegation uses inventory vars, works fine).
- Patch to 1.34.3 (kubespray v2.30 ceiling), then submodule→v2.31 + upgrade-cluster to 1.35.x before October; etcd/CoreDNS/containerd ride along. Pre/post checklist = the M75 issuer/api-audiences check, multus DS restart, cni-dir owner, per-kernel modules. Quarterly cadence thereafter. **Effort: M.**


### ✅ M125. unifi TF provider MIGRATED to ubiquiti-community/unifi 0.41.25 — DONE 2026-07-02 (import-based, plan=No changes, CI green); the fork FIXED the PUT-400 bug
- **Schema mapping researched (fork 0.41.25, from its repo docs):** it is a complete resource rewrite, NOT arg renames: `dhcp_enabled/dhcp_start/dhcp_stop/dhcp_lease` → nested `dhcp_server = {enabled,start,stop}` (lease TBD — check `dhcp_server` nested schema); `vlan_id` → `vlan`; `internet_access_enabled` → `internet_access`; `intra_network_access_enabled` → **`network_isolation` (INVERTED semantics — isolation=true ≈ intra_access=false; verify!)**; `purpose`/`network_group` GONE (purpose implied; `third_party_gateway` flag exists); `subnet` example format is host-style `10.0.0.1/24` (verify vs our network-style `10.10.199.0/24` — a silent diff source); `domain_name`/`igmp_snooping`/`multicast_dns` keep their names. `unifi_user`/`unifi_port_forward` schemas need the same doc pass.
- **⚠️ WHY replace-provider CANNOT work even with correct .tf:** the fork can't upgrade state written by paultyng's schema (structurally different attributes) → TF plans REPLACE of all 7 networks = recreating live VLANs (Servers VLAN carries everything). **The safe recipe is import-based:** (1) new branch: rewrite networks.tf/reservations.tf/port-forwards.tf to the fork schema; (2) `terraform state rm` each unifi_* resource; (3) `terraform import` each by its controller ID (grab IDs from state first: `terraform state pull | jq`); (4) iterate until `plan = 0/0/0`; (5) then remove the M110 `ignore_changes` and see if the fork fixed the PUT-400s. Needs the lifecycle runner (UDM creds) + a window where a bad import can be re-imported calmly. **Effort: M-L. Owner: agent + window.**
- Historical: 2 same-day swap attempts cleanly reverted (0.54 renamed resource types; 0.41.25 rewrote args). paultyng still serves + works with workarounds — no urgency.
- **2026-07-02 ATTEMPT + CLEAN REVERT:** tried `ubiquiti-community/unifi` **0.54.0** (the version the research called drop-in). It is **NOT** — 0.54 **renamed the resource types**, so the plan errored `provider does not support resource type unifi_network/unifi_port_forward/unifi_user` + `Invalid Attribute Value`. The workflow's `state replace-provider` had **already rewritten S3 state** before the plan failed → reverted via a reverse `replace-provider` step (run 28600745760 = success, state back on paultyng, plan clean) + removed the step. **No infra changed** (plan-only failure). **Lesson:** a provider-source swap runs replace-provider BEFORE plan validates schema compat — so a non-drop-in fork half-migrates state; always test the fork's resource-name compat first (e.g. a scratch `terraform plan` against a state copy), and keep the reverse replace-provider ready.
- **Re-attempt options:** (a) pin the ubiquiti-community fork to a version that still matches paultyng 0.41's resource names (check its early tags), OR (b) accept 0.54's renamed resources and `terraform state mv` each resource to the new type + update the .tf, OR (c) try the **akerl/unifi** fork. Investigate which fork/version keeps `unifi_network`/`unifi_port_forward`/`unifi_user` before swapping. **Effort: M (was mis-scoped as S).**
- **Attempt 2 (same day, also cleanly reverted):** pinned the fork's own **0.41.25** (its 0.41.x line) — resource NAMES survive but the **`unifi_network` arguments changed** ("Unsupported argument" ×20 on networks.tf) → not drop-in at ANY fork version. Both reversals verified green (state on paultyng, plan clean). **Conclusion:** M125 = adapt networks.tf/port-forwards/reservations to the fork's schema (attribute diff, maybe state mv) as a deliberate project. The archived paultyng provider still serves from the registry + works with the M110 `ignore_changes` workarounds — no urgency. Original context:
- paultyng/unifi archived 2026-04-30; it now deterministically 400s PUT networkconf for 3 of 7 networks (bit the M110 dhcp_dns cutover — worked around via direct UDM API + ignore_changes). Fork is drop-in. Swap `source`, re-init, verify plan `0/0/0`, then try removing the M110 ignore_changes. **Effort: S.**


### ✅ M127. Tailnet split-DNS still lists the DESTROYED dns-aws (10.10.100.5) — DONE 2026-07-02 (console edit `.5`→`.100.10`; verified 1–3 ms via 100.100.100.100). Residuals optional: tailnet-DNS-into-IaC (S) + the off-cluster second subnet router decision (S). Original finding:
- **Found 2026-07-02** diagnosing the owner's on-Tailscale failures reaching `cue.etherport.net`. The Tailscale admin console's split DNS for `etherport.net` forwards to **10.10.201.5, 10.10.201.6, 10.10.100.5** — but `.100.5` was destroyed by M110 (`68a8156`) on 07-01. Verified from the devbox: `.100.5` times out; the consolidated edge box **10.10.100.10 answers correctly over the tailnet** (NOERROR, Cloudflare IPs). Every on-TS `etherport.net` lookup now races a dead resolver → intermittent failures/slow first loads. The outage windows themselves line up with today's cluster maintenance (cilium H45a + restart waves took down the technitium pods, MetalLB BGP announcements, AND the in-cluster `ts-homelab-subnet-router` — the tailnet's only path to VLAN 201 — simultaneously; home-LAN DHCP DNS points at the same VIP first, so WiFi-no-TS degraded too).
- **Fix (console, 2 min):** Tailscale admin → DNS → split DNS `etherport.net`: replace `10.10.100.5` → `10.10.100.10`. Not terraform-managed (no tailscale module) — consider adopting the tailnet DNS config into IaC so the next resolver move can't strand it (S, optional).
- **Resilience note:** the subnet-router pod runs on the same cluster whose DNS it fronts — during cluster maintenance, on-TS users lose BOTH the resolvers and the route to them. Mitigation options: a second subnet router off-cluster (e.g. a standalone VM), or accept + schedule maintenance windows. **Effort: Trivial (console edit) + S (IaC/second-router decisions).**



> M112–M121 + L25–L31 below are from the **2026-07-01 Fable-5 infra review** (3 parallel read-only agents:
> K8s efficiency/hardening, AWS, CI/CD, ansible, observability, cost). Deduped against everything above.


### ✅ M112. `concurrency:` groups on all terraform workflows — DONE 2026-07-01 (`72cb120`): `tf-<stack>` group, `cancel-in-progress: false`, added to all 24 TF workflows (incl. drift-detection).
- 0 of 25 TF workflows have one — a dispatch apply + push-plan on the same stack race the S3 lock (bit twice on 2026-07-01: us-east-1 destroy vs push-plan; plus a stale `.tflock` from a killed 06-17 run silently failed that stack's plans for 2 weeks). **Fix:** `concurrency: {group: tf-<stack>, cancel-in-progress: false}` per workflow. **Effort: S.**


### ✅ M113. Alert-coverage gaps — DONE 2026-07-01 (`727a40d`,`47e29b7`), verified: cert-manager ServiceMonitor enabled (metrics were never scraped!); NEW 13-service-alerts.yaml — CNPGClusterDown ×2 (absent()-based), CertManagerRenewalOverdue + CertExpiryCritical (**duration-agnostic — the wildcard is a ~6.7d short-lived LE cert, absolute-day thresholds false-fired within minutes and were rewritten to renewal-overdue semantics**), AuthentikDown (new in-cluster blackbox Probe + plain http_2xx module), CloudflaredTunnelDegraded/Down; runbook_url pass (14 rules linked to their docs/runbooks/alerts docs). All 7 rules loaded + inactive on the healthy cluster.
- CNPG has only backup alerts — a dead `postgres-cluster`/`cue-db` never pages; no general `certmanager_certificate_expiration_timestamp` alert; authentik (gates all admin UIs) and cloudflared (all external ingress) have zero rules. Also **0 of 82 rules carry `runbook_url`** even though `docs/runbooks/alerts/` has per-alert docs named after the alerts — the advisor emails lose that context. **Fix:** ~4 new rules (metrics already scraped) + a mechanical annotation pass + a lint for new rules. **Effort: S.**


### ✅ M114. authentik-server HA — DONE 2026-07-01 (`72cb120`), verified: replicas 2 on distinct nodes (w1+w4), zero-gap RollingUpdate, hostname topologySpread, PDB minAvailable 1, worker `ak healthcheck` probes. **Unblocked by dropping the RWO media PVC** (it held only the initContainer-regenerated login-bg.png → emptyDir now; future real media = RWX/S3 decision, don't re-add RWO). In-cluster `/-/health/ready/` = 200.
- Same failure class as the just-fixed cue-api: every drain of its node takes down SSO (Grafana/wiki/Open WebUI + all forward-auth admin UIs). Server is stateless (shared HA postgres + redis) → safe at 2. **Fix:** replicas 2 + PDB minAvailable 1 + topologySpread (mirror `493868b`) + liveness/readiness on the worker. **Effort: S.**


### ✅ M115. authentik = 6th NetworkPolicy-enforced tier — DONE 2026-07-01 (`38da9a9`), verified: `15-tier-authentik.yaml` (ingress :9000 from traefik+blackbox-exporter+intra-ns; egress postgres :5432 + SES :587 + intra-ns; world :443 deliberately absent — update-check/analytics/gravatar all disabled). Full audit-toggle procedure: audit ON → label → 605 real flows observed, **0 would-be drops** → audit OFF → enforced-path verified (traefik-pod→authentik OK, probe_success=1, 0 DROPPED).
- The SSO IdP (crown-jewel credential system) sits allow-all while `cue,dns,monitoring,postgres,traefik` are enforced; it's already a postgres-tier client so half its flows are mapped. **Fix:** the documented audit-on→observe→enforce procedure (`docs/runbooks/networkpolicy-tiers.md`). **Effort: M.**


### ✅ M116. k8s-node automated security patching — DONE 2026-07-01 (`f4bc0dd`), applied to all 8 nodes + spot-verified: NEW `k8s-unattended-upgrades.yml` (Ubuntu -security pocket only, `Automatic-Reboot=false`) — **kured owns reboots** (existing deploy: concurrency 1, nightly 02:00-06:00 PT window, watches /var/run/reboot-required, cordon+drain). kubelet/containerd are kubespray binaries, not apt packages — untouchable by the security origin.
- `base.yml` is `hosts: all:!k8s_cluster` — nodes get no unattended-upgrades and none of the sshd baseline; the only patch path is the manual `k8s-node-patch.yml`. **Fix:** either a scheduled (monthly) dispatch of the rolling patch playbook, or a security-only unattended-upgrades profile on `k8s_cluster` with reboots left to kured. **Effort: M.**


### ✅ M117. metrics-server — DONE 2026-07-01 (`727a40d`), verified: HelmRelease (chart 3.13.0, kube-system, kubelet-insecure-tls) — `kubectl top nodes/pods` now works cluster-wide.
- Blocks HPA, incident triage when the monitoring ns itself is down, and utilization-aware tooling. **Fix:** metrics-server via kubespray flag or a small HelmRelease. **Effort: S.**


### ✅ M118. Resource right-sizing — DONE 2026-07-01 (7d PromQL evidence), rolled + verified: velero node-agent 200m/256Mi→**25m/160Mi** (7d max incl. backups = 138Mi; freed ~1.4 CPU + ~0.8Gi schedulable); prometheus 512Mi req/2Gi lim→**2Gi/3Gi** (P95 1.83Gi — was <10% from OOM); alloy 128Mi→**384Mi** (P95 315Mi); tetragon 32Mi(kyverno-default)→**256Mi** (P95 219Mi; the HR requests hadn't been landing — fresh upgrade fixed it).
- PromQL vs requests: 8× velero node-agent ~200m/200Mi each over-requested (~1.6 CPU + 1.6Gi reserved idle fleet-wide); prometheus runs ~1Gi ABOVE its 512Mi request, alloy ~175-217Mi over 128Mi/node, tetragon 50-155Mi over — under-requested pods burst into headroom the scheduler thinks is free and are first-evicted under pressure. **Fix:** lower node-agent, raise the three under-requesters to observed P95. **Effort: S.**


### ✅ M119. Backup thundering-herd stagger — DONE 2026-07-01 (`727a40d`,`6fa4f2d`), verified live: 7 s3-sync shares 01:00→01:50 at 10-min steps (scans suspended but slotted at 02:00); velero de-stacked to :00/:20/:40 (02:00,04:00 groups) + :00/:12/:24/:36/:48 (03:00 group).
- All six s3-sync CronJobs fire at `0 1 * * *` (simultaneous UNAS+WAN hammer + shared-lock contention); 5 velero Schedules at exactly `0 3 * * *` drive the fs-backup/CNPG spike — same fan-out pattern that fed the 10:00 UTC etcd cascade. **Fix:** stagger minutes (01:00/01:15/… and 03:00/03:20/…). **Effort: S.**


### ✅ M120. ceph-csi codified + moved to its namespace — DONE 2026-07-01 (`8851750`), e2e-verified. Bigger than reported: the workloads (deploy/DS/SA/RBAC) ran 50d as an **out-of-band kubectl apply** (not in git at all), and the ceph-csi-ns configmap copy pointed at the **pre-VLAN-migration monitor 10.10.201.41** — a naive move would have broken all new volume ops. Codified the full stack from a cleaned live dump into `storage/ceph-csi/` (ns ceph-csi; git configmaps with the correct 10.10.210.41 overwrote the stale copy). Cutover: old deleted → Flux applied new → **one gotcha: the old pods' termination deleted the new registrar's socket** (plugins_registry emptied post-registration) → DS restart re-registered 5/5 → **acid test green** (PVC provision→attach→mount→write→delete all through the moved stack; 22 existing Bound PVCs unaffected). default ns now EMPTY + PSS enforce=baseline (adopted as a git resource in policy-baseline/); 50d rbd-test-pvc deleted; provisioner PDB moved with the workload.
- The privileged CSI DS + provisioner run in `default`, which has no PSA label (anything landing there gets zero admission guardrails); the intended `ceph-csi` ns exists but is empty; `default/rbd-test-pvc` (1Gi) has lingered 50d. **Fix:** migrate CSI into `ceph-csi` (careful — storage path), or at minimum PSA-label `default`, delete the test PVC + decide the empty ns. **Effort: M (move) / S (label+PVC).**


### ✅ M121. Drift plan redaction — DONE 2026-07-01 (`35dce3a`): plan.txt redacted to structure-only lines (Plan: summary + resource headers) before artifact upload AND the drift-issue embed; attribute values no longer leave the run.
- Non-`sensitive` attributes render into plan text; artifacts are downloadable with repo read. **Fix:** stop uploading raw plans (step-summary tail suffices) or scrub. **Effort: S.**


### ✅ M111. AWS cost spike root-caused + fixed — velero Kopia hourly maintenance → S3 request storm
- **Trigger:** cost alert ~$160 forecast (June ~$138 vs April ~$107). User suspected S3 storage-class transitions failing / archive bloat — **disproven**: the 10 TB `archive` bucket is legit (6 shares, `media/`=7 TB) and correctly in Deep Archive (~$10/mo); storage across all 15 buckets ≈ $17/mo; **0 noncurrent versions**.
- **Root cause:** the spike is S3 **REQUEST** cost. velero's Kopia repo-maintenance ran **HOURLY** (velero default; 11 `BackupRepository` CRs at `maintenanceFrequency: 1h0m0s`) — each run LISTs/GETs/rewrites the whole repo index in S3. ×11 repos ≈ 7,200 runs/mo ≈ **~$70/mo**. `velero` bucket born 2026-05-14 == the May spike month.
- **✅ Fixed (`e9f11d3`):** `defaultRepoMaintainFrequency: 24h` in the velero HelmRelease (verified server arg) + **live-patched the 11 CRs to `24h0m0s`** (velero owns them, not Flux) → ~24x fewer maintenance runs, effective immediately. Also added the missing `logs.grahamsmith.net` lifecycle (auto-expires ~116k dead ALB access-log objects; applied via `terraform-s3`, plan `1 add/0 destroy`).
- **Target ≈ $35/mo** (`aws-cost-teardown` workflow). **⏳ Remaining levers:** M110 consolidation (below) + us-east-1 decom (≈$9/mo). **⏳ USER-only** (CE/wafv2 denied to the scoped key): confirm the S3 `Requests-Tier1/2` line in Cost Explorer (Group-by Usage Type) + optionally grant `ce:GetCostAndUsage` + a Budget alert; console-check for a stray REGIONAL WAFv2 WebACL ($5/mo if orphaned). **⏳ Architectural follow-up (3-2-1):** move velero's PRIMARY BSL to LOCAL MinIO/Ceph RGW (zero-request frequent backups) + weekly BATCHED rclone → Deep Archive for DR. See session-log 2026-07-01.


### ✅ M110. AWS vpn+dns consolidation — COMPLETE 2026-07-02
- **End state (verified live):** ONE AWS box — `private-infra_edge` (t4g.small) + ONE EIP `44.240.60.80` serving WireGuard (wg0+wg1) + Tailscale + **Technitium DNS** (folded via `technitium.yml`; 47 records synced; answers internal names on the EIP + `.10`). The standalone dns instance + EIP `52.40.219.113` + its 3 CW alarms **destroyed** (plan-gated `5 to destroy`); us-east-1 + travel tooling already gone. UniFi `dhcp_dns` re-pointed on all 7 VLANs (4 via TF; **3 via direct UDM API PUT — the archived paultyng provider deterministically 400s on clients/vsan/ceph**, now `ignore_changes` + comment; stack back to `No changes`). dns SG (with the Lambda-managed :53 WAN allows) attached to the edge instance. All `.5`/dns-aws references cleaned (scrape, dns-sync, dns-tier netpol /32, inventory). Cert-SSH trust installed on the box (CI static-key branch remains until the M76-parity cutover — ⚠️ residual below).
- **✅ Residuals CLOSED 2026-07-02:** (1) **M76 parity DONE** — step-ca-trust applied to the edge box (3 failed runs root-caused: NOT the WAN wave but **sudo stalling 12s+ on a hostname lookup** — `ip-10-10-100-10` was missing from /etc/hosts post-M110; fixed on-box + ANSIBLE_TIMEOUT=60 kept as belt) → cert-SSH verified from devbox + CI → ansible-vm-fleet aws branch flipped to cert (awskey step deleted) → `step-ca-remove-static-key` applied → **verified: cert works, static key GONE from authorized_keys** (survives only as the cloud-init rebuild seed; break-glass = SSM). (2) **F1+F7 applied** (dead `internal_aws_spokes` /19 rule + orphaned `alb-public-443` SG deleted; run 28609440745); F2/F3 port-scoping = the deliberate consolidated-SG redesign, still ⏳. (3) **doc sweep DONE** (12 docs reconciled + the `.5`→`.10` live-ref fix).


### ✅ L25. DONE 2026-07-01 (`35dce3a`) — test-ssh-cert.yml deleted; permissions:{} on bootstrap-runner-key.yml.
- The former still consumes `STEP_JWK_PASSWORD` on the self-hosted runner post-cutover; the two are the only workflows with no `permissions:` block. (2026-07-01 review #14.) **Effort: S.**


### ✅ L26. automountServiceAccountToken sweep — DONE 2026-07-02: false on plex, wikijs, ollama, open-webui, technitium, home-assistant (rolled via Flux).
- 243/255 pods automount a SA token; plex/wikijs/ollama/technitium/home-assistant etc. never call the API — a pod compromise hands each a valid cluster credential. (#15.) **Effort: M.**


### ✅ L27. DONE 2026-07-01 (`35dce3a`), verified live: enforce=baseline on kyverno + plex (the plex "GPU needs privileged" note was STALE — device-plugin GPU, dry-run clean); pg-recovery ns deleted (held a stray csi-rbd-secret COPY — bonus credential cleanup).
- Free hardening; velero/tetragon/home-automation legitimately can't (hostPath/privileged). (#16.) **Effort: S.**


### ✅ L28. DONE 2026-07-01 (`35dce3a`) — no_log on the 3 token-in-URL tasks; structurally verified every technitium_token task now carries it.
- They pass `?token={{ technitium_token }}` in the URL — the admin DNS token lands in ansible output on failure/`-v` (the user/password tasks nearby DO have `no_log`). (#18.) **Effort: S.**


### ✅ L29. Loki per-stream limits — DONE 2026-07-01: per_stream_rate_limit 3MB/10MB-burst + 7d retention_stream for hubble-audit + tetragon export (selectors verified against the actual ruler rules). loki-0 restarted clean.
- Global limits only today — a hubble-audit/tetragon runaway can eat the whole 10MB/s tenant budget; 30d retention applies uniformly to high-volume audit streams. (#19.) **Effort: S.**


### ✅ L31. Minor hygiene batch — COMPLETE 2026-07-02: email_fwd abort-MPU + SSE-S3 blocks codified on all 7 default-reliant buckets (plan-gated `7 to add`, applied); WG-key item moot.
- `email_fwd` S3 bucket lacks the abort-incomplete-MPU rule every other bucket has; ~~WG private key passed as CLI `-var` in `terraform-regional-vpn.yml`~~ (moot — workflow deleted 2026-07-01); codify SSE blocks on the 7 buckets relying on AWS-default SSE-S3 (parity only). **Effort: S.**


### ✅ L2. Regional VPN destroy doesn't drop the per-region Route53 record — CLOSED (moot 2026-07-01)
- **Moot:** the entire travel-VPN tooling (`aws-regional-vpn/` stack + `modules/regional-vpn/` + workflow) was **deleted 2026-07-01** (M110 travel-tooling cleanup; both workspaces held 0 resources, S3 states removed). Nothing left to fix.
- **Status 2026-05-24 (historical):** module-side fix shipped, workspace-side migration pending.
- **Done:** `infra/terraform/modules/regional-vpn/main.tf` now accepts `dns_zone_id` + `dns_record_name` variables; when both are set (and `use_elastic_ip = true`), creates an `aws_route53_record.vpn_endpoint` resource that destroy-cleans automatically. Output `dns_record_fqdn` exposes the created FQDN.
- **Still TODO:** the live `infra/terraform/aws-regional-vpn/` workspace doesn't use the modules/regional-vpn module — it's a self-contained set of resources that uses `associate_public_ip_address = true` (no EIP, so IP changes on every instance reboot, defeating the DNS-record point). Migrate that workspace to either (a) use the module with `use_elastic_ip = true` + `dns_zone_id`/`dns_record_name`, or (b) duplicate the EIP + Route53 record resources inline. (a) is cleaner but is a one-shot state-migration exercise (destroy + re-apply, accepting brief outage on the next regional spin-up).
- **Original entry:**


- **Source:** observed during M38 (Mumbai destroy 2026-05-23). After `terraform-regional-vpn.yml` action=destroy ran clean, the `vpn-travel.etherport.net. → 13.234.119.106` A record was still present (deleted by hand after). The regional-vpn module manages the EC2/VPC/SG side but doesn't own the public DNS record — that lives in the `route53` module as a separate resource keyed on the EIP. So destroy leaves a dangling record pointing at a freed-up Elastic IP.
- **Risk:** the next AWS customer to grab that EIP would receive traffic addressed to `vpn-travel.etherport.net` until the record's 300s TTL expires. Low real-world impact for a WireGuard endpoint (handshake fails without the right key) but still a leak.
- **Fix options:** (a) move the Route53 record into the regional-vpn module so the per-region apply/destroy owns it end-to-end; (b) wire a `data` reference + `null_resource` `destroy_provisioner` in the regional-vpn module to call `aws route53 change-resource-record-sets DELETE` on teardown; (c) accept it as a manual step in the resurrection runbook. (a) is cleanest.
- **Effort:** S.

