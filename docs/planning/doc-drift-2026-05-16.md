# Doc Drift Audit — 2026-05-16

Scope: all `*.md`, `README*`, and `docs/**`. Drift checked against the post-rebuild cluster (3 CP + 4 workers + 1 GPU), Packer VM 9001 template, `ubuntu` user + auto-key, and recent platform changes.

## Files with drift

### `/Users/grahamsmith/code/infra/docs/architecture/overview.md`
- Cluster shape wrong: "1 control-plane + 4 workers"; should be 3 CP + 4 workers + 1 GPU.
- IP table missing cp2/cp3 (.51/.52), w4 (.56); k8s-gpu1 listed as .54 — actual is .60. w1/w2/w3 listed at .51/.52/.53 — actual is .53/.54/.55.
- Ingress line says "DNS-01 Route53" — Traefik no longer does ACME; cert-manager wildcard + TLSStore now owns TLS.

### `/Users/grahamsmith/code/infra/docs/runbooks/PLATFORM-MANAGEMENT.md`
- ASCII diagram shows 1 cp + 3 workers + gpu1 (missing cp2/cp3, w4).
- Standalone-VMs panel missing `gh-runner` (10.10.201.x VM 1003).
- `ssh graham@10.10.201.15` in health check — should be `ubuntu` + `~/.ssh/auto-key` (vpn-local rebuild pending Task #4, but doc should call this out).

### `/Users/grahamsmith/code/infra/docs/runbooks/operations-guide.md`
- All `ssh graham@...` examples (lines 329, 339, 540, 547) wrong for K8s nodes — they're `ubuntu` now. vpn-local/dns-fallback still `graham` until rebuild.
- No mention of hardware watchdog, CNPG HA, Velero schedules in git, post-bootstrap.sh.
- "Restart wg-quick" on K8s now means restarting the K8s WireGuard pod, not the systemd unit.

### `/Users/grahamsmith/code/infra/docs/runbooks/disaster-recovery.md`
- §1.2 `ssh graham@10.10.201.50` — should be `ubuntu`.
- §3.1 cluster description says "dns1, dns2 (K8s), dns-fallback, dns-aws" but K8s pods are `technitium-0/1` at .71/.72.
- §3.2 `ssh graham@10.10.201.6` — dns-fallback still graham, OK for now but flag.
- §1.3 cluster-rebuild procedure is missing the static-PV recovery pattern that's now canonical (CNPG `03-static-pv-recovery.yaml` / `04-pvc-pre-bind.yaml`) — only mentions Velero restore, which would re-`initdb`.

### `/Users/grahamsmith/code/infra/docs/runbooks/kubernetes-upgrade.md`
- Worker loop (§2.4) uses `k8s-w1 k8s-w2 k8s-w3 k8s-gpu1` — missing k8s-w4.
- Lines 250, 300, 318: `ssh graham@10.10.201.50` — should be `ubuntu`.
- Pre-upgrade etcd backup section assumes single CP — should reference 3-CP HA backup pattern (now in etcd-backup-restore.md §HA Cluster Restore but not cross-linked).

### `/Users/grahamsmith/code/infra/docs/runbooks/k8s-ha-migration.md`
- Whole doc describes the migration that already happened (2026-05-12). Either delete, or move to `planning/archive/` with a "completed" banner.
- VM template ID line says "(VM 9000)" — wrong, should be 9001.
- Old IP column claims w1=.51 — but the migration has shipped, so this is historical only.

### `/Users/grahamsmith/code/infra/docs/runbooks/instance-migration.md`
- VPN/DNS instance migration steps still SSH as `graham@10.10.201.15` and `graham@10.10.201.6` — those VMs aren't rebuilt yet, so OK. Add a "rebuild-pending" note pointing to Task #4.
- No mention of i6300esb watchdog reattach requiring stop+start of imported VMs (covered in vm-watchdog.md but not cross-linked here).

### `/Users/grahamsmith/code/infra/infra/packer/ubuntu-cloud-init/README.md`
- Whole "Usage" section mixes 9000 vs 9001. Line 3 says template is VM 9000 — wrong; 9000 is the raw cloud-image base, 9001 is the Packer-built template.
- §3, §4, "Integration with Terraform" all reference `vm_id = 9000` for clone source — should be 9001.
- `ssh_username` table row says `graham` — Packer now bakes the `ubuntu` cloud-init user with the auto-key.
- Missing: netplan/Multus VLAN parents now baked into the build.

### `/Users/grahamsmith/code/infra/infra/kubespray/README.md`
- "Current Cluster" table missing cp2/cp3, w4. k8s-w3 listed at .53 — that IP is now w1.
- §"Kubeconfig" `scp graham@k8s-cp1:` — should be `ubuntu`.
- Inventory paths reference `inventory/wind/` — workflow now uses `inventory/inventory.ini` at the kubespray dir root.

### `/Users/grahamsmith/code/infra/docs/reference/node-vlan-setup.md`
- Whole doc describes `eth1/eth2/eth3` + manual `systemd-networkd` setup; reality is `enp6s19/20/21` + netplan baked into Packer + ansible playbook.
- Node table missing w2/w3/w4; calls gpu1 .53 (now .60).
- Should be redirected to `docs/runbooks/vlan-interfaces-netplan.md` + `platform/kubernetes/multus/README.md`, or rewritten as a quick-reference for the netplan file.

### `/Users/grahamsmith/code/infra/platform/kubernetes/home-automation/README.md`
- Multus prereq references `KUBESPRAY_MIGRATION.md` — fine, but mentions only w1/w2/w3/gpu1 (missing w4).
- "Cluster network (Calico)" — actually Cilium.
- Links to non-existent paths: `docs/kubernetes/node-vlan-setup.md`, `docs/SOPS-SETUP.md` (real paths are `docs/reference/node-vlan-setup.md` and `docs/setup/secrets/SOPS-SETUP.md`).

### `/Users/grahamsmith/code/infra/platform/kubernetes/traefik/README.md`
- "Certificate Management: ACME/Let's Encrypt via Route53 DNS-01" — now cert-manager wildcard + TLSStore; Traefik HA (replicas=2, dropped ACME PVC).
- `pvc-traefik-ceph.yaml` listed as a file — file's still in dir but no longer used (ACME PVC dropped). Confirm + remove from doc.

### `/Users/grahamsmith/code/infra/platform/kubernetes/cnpg/README.md`
- "Read-only replicas (optional)" — instances=3 is now the standard.
- "Backup Configuration (Optional)" trailing section duplicates what's now live and runbook'd (postgres-barman.md). Should link out instead of showing as optional.
- Operator version `0.22.1` — check against current Helm release.

### `/Users/grahamsmith/code/infra/platform/kubernetes/backups/velero/README.md`
- "Schedule Files: postgres-daily.yaml, wikijs-daily.yaml" — now 9 schedules in `schedules/`, all in git per commit 95b3755. Update list.
- "Other schedules were created via CLI" — no longer true.

### `/Users/grahamsmith/code/infra/platform/kubernetes/plex/README.md`
- "Cilium pod network 10.42.0.0/16" — verify; default is 10.233.64.0/18 per kubespray config.
- Image tag `1.40.0.7998` example is stale (Renovate manages tag now).
- "Created: 2025-12-31" footer — drop or update.

### `/Users/grahamsmith/code/infra/platform/kubernetes/wireguard/README.md`
- "Pod prefers k8s-w1" — still correct, but should mention VRRP failover correctness fix (commit b4999c9) and HA Postgres-style replicas if relevant.
- OK overall.

### `/Users/grahamsmith/code/infra/platform/kubernetes/auto-remediation/README.md` & `docs/runbooks/auto-remediation/COVERAGE.md`
- COVERAGE.md "Last Updated: January 4, 2026" / "17 deployments + 3 statefulsets / 22 alerts" — verify against current PrometheusRule set; CNPG, gpu-operator, cert-manager additions likely shifted these.
- New components not covered: CNPG cluster health, barman archiving, Velero schedule failures.

### `/Users/grahamsmith/code/infra/platform/technitium/README.md`
- Cluster diagram lists `10.10.201.5` as "Kubernetes cluster VIP (primary)" and standalones as secondary — consistent with `platform/kubernetes/technitium/README.md`, OK.
- No mention of `aws.etherport.net` private zone + Route53 Resolver inbound endpoint forwarders (covered in `docs/runbooks/aws-private-dns.md` only).
- No mention of Route53 sinkhole records for `pve` / `ceph` under `wind.etherport.net` (commit d542853).

### `/Users/grahamsmith/code/infra/docs/architecture/aws-infrastructure.md`
- "Backup Strategy" doesn't mention CW agent now publishes only 2 metrics (was 15) — shift to Prometheus monitoring.
- WAF section doesn't mention 7d log retention.
- Missing: `aws.etherport.net` private hosted zone, `postgres-barman.wind.etherport.net` bucket, S3 bucket-policy denies for `DeleteBucket`/`DeleteBucketPolicy`.
- "Internal Communications SG" rules look stale post-spoke-VPC update (commit 8764631).

### `/Users/grahamsmith/code/infra/docs/architecture/network.md`
- 4-line "Current Configuration" table omits VLAN 202/204/205 in-cluster reachability via Multus (covered in node-vlan-setup but not surfaced here).

### `/Users/grahamsmith/code/infra/docs/setup/gitops/flux-overview.md`
- "approximately 48% of applications are managed via Flux" — stale; nearly everything is Flux-managed now (cert-manager, monitoring, CNPG, gpu-operator, etc. all HelmReleases).
- "Not Flux-Managed (manual deployment): Velero, Kured, Prometheus/Grafana, Cert-Manager" — all Flux-managed now per `clusters/wind/helm-releases/`.

### `/Users/grahamsmith/code/infra/docs/runbooks/vlan-interfaces-netplan.md`
- Mostly correct since 2026-05-12 rewrite. "Step 3" header dangles inside the emergency block (formatting bug). Manual workaround uses both `ens19` and `enp6s19` — should pick one (enp6s19).

### `/Users/grahamsmith/code/infra/infra/ansible/KUBESPRAY_MIGRATION.md`
- Dated "Changes Made (2026-01-01)" and presents migration as future work. Reality: shipped. Mark as historical or fold key bits into kubespray README.

### `/Users/grahamsmith/code/infra/docs/planning/K8S-W3-DEPLOYMENT-PLAN.md`
- Superseded by the HA migration. Move to archive or delete.

### `/Users/grahamsmith/code/infra/docs/setup/kubernetes/ingress-traefik.md`
- 33 lines, says "TLS via ACME DNS-01 using Route53" — wrong now (cert-manager wildcard).
- References `pvc-traefik-ceph.yaml` and `traefik-acme-fix.yaml` which are no longer relevant.

### `/Users/grahamsmith/code/infra/docs/setup/kubernetes/cluster-build-kubespray.md`
- Refers to `inventory/wind/` path; kubespray inventory layout was reorganized.

## Missing docs

- **Hardware watchdog (i6300esb)**: covered by `vm-watchdog.md` already (commit 72b4941). OK.
- **Multus VLAN parent setup in Packer**: covered in `multus/README.md`. OK.
- **cert-manager wildcard + TLSStore**: no runbook. Needed — describe how the cluster-issuer + wildcard cert + Traefik TLSStore wire together and how to renew/rotate.
- **CNPG instances=3 + Barman**: barman activation runbook exists (`postgres-barman.md`). Lacks day-2 ops (failover testing, switchover, monitoring continuous-archiving lag).
- **Velero schedules in git**: README lists only 2 of 9. Update + document the kustomization pattern for schedules.
- **Postgres static-PV recovery**: codified in `cnpg/README.md` §Disaster Recovery + manifests. Not surfaced in `disaster-recovery.md`.
- **Route53 sinkhole + `aws.etherport.net`**: aws-private-dns.md covers the zone. Sinkhole records (commit d542853) are undocumented — note in `technitium/README.md` or new short doc.
- **Prometheus AWS monitoring shift** (commit 69ac7dd): no doc covers the new exporters / scrape targets / status-check recovery alarm.
- **WAF 7d log retention**: just a config detail, mention in `aws-infrastructure.md`.
- **Post-bootstrap.sh + autonomous-run decisions** (commit b390dee): scripts exist; no doc describes when/why to run them.
- **gh-runner VM (1003)**: no README. Add a short doc covering the ansible playbook (`infra/ansible/playbooks/gh-runner.yml`) and the workflow it serves.

## OK as-is (reviewed, no drift)

- `/Users/grahamsmith/code/infra/README.md`
- `/Users/grahamsmith/code/infra/docs/README.md` (index — links resolve)
- `/Users/grahamsmith/code/infra/docs/architecture/firewall-zones.md`
- `/Users/grahamsmith/code/infra/docs/architecture/vpn-tailscale.md`
- `/Users/grahamsmith/code/infra/docs/architecture/vpn-wireguard.md`
- `/Users/grahamsmith/code/infra/docs/runbooks/UPDATE-PROCEDURES.md`
- `/Users/grahamsmith/code/infra/docs/runbooks/dns-resolution-issues.md`
- `/Users/grahamsmith/code/infra/docs/runbooks/etcd-backup-restore.md` (covers HA case correctly)
- `/Users/grahamsmith/code/infra/docs/runbooks/gpu-secureboot.md`
- `/Users/grahamsmith/code/infra/docs/runbooks/postgres-barman.md`
- `/Users/grahamsmith/code/infra/docs/runbooks/regional-vpn-deployment.md`
- `/Users/grahamsmith/code/infra/docs/runbooks/proxmox-ha-expansion.md`
- `/Users/grahamsmith/code/infra/docs/runbooks/vm-watchdog.md`
- `/Users/grahamsmith/code/infra/docs/runbooks/aws-private-dns.md`
- `/Users/grahamsmith/code/infra/docs/runbooks/auto-remediation/README.md`, `SETUP.md`
- `/Users/grahamsmith/code/infra/docs/setup/gitops/repo-workflow.md`, `making-changes.md`
- `/Users/grahamsmith/code/infra/docs/setup/github-actions/README.md`
- `/Users/grahamsmith/code/infra/docs/setup/secrets/SOPS-SETUP.md`, `1PASSWORD-CLI.md`
- `/Users/grahamsmith/code/infra/docs/setup/network/ubiquiti-ddns.md`
- `/Users/grahamsmith/code/infra/docs/setup/terraform/*` (proxmox-k8s-vms.md, remote-state-backend.md, aws-security-best-practices.md)
- `/Users/grahamsmith/code/infra/docs/setup/kubernetes/storage-ceph-csi.md`, `addons-metallb.md`, `monitoring-kube-prometheus-stack.md`
- `/Users/grahamsmith/code/infra/docs/reference/kubectl-cheatsheet.md`, `kustomize-patterns.md`
- `/Users/grahamsmith/code/infra/docs/operations/terminal-setup.md`
- `/Users/grahamsmith/code/infra/docs/guides/vpn-split-tunnel.md`, `localtuya/*`
- `/Users/grahamsmith/code/infra/docs/planning/long-term-stability-review-2026-05-12.md`, `migration-questions-2026-05-12.md` (dated session reports — keep as-is)
- `/Users/grahamsmith/code/infra/docs/planning/{aws-cost-analysis,github-actions-automation-roadmap,INFRASTRUCTURE-HARDENING-CHECKLIST,PRODUCTION-READINESS-CHECKLIST,public-web-infrastructure,sops-vs-ansible-vault,TODO-configmap-migration,VERSIONING-STRATEGY}.md` (planning docs — drift acceptable)
- `/Users/grahamsmith/code/infra/platform/kubernetes/multus/README.md` (rewritten 2026-05-12)
- `/Users/grahamsmith/code/infra/platform/kubernetes/metallb/README.md`
- `/Users/grahamsmith/code/infra/platform/kubernetes/kured/README.md`
- `/Users/grahamsmith/code/infra/platform/kubernetes/technitium/README.md`
- `/Users/grahamsmith/code/infra/platform/kubernetes/ollama/README.md`
- `/Users/grahamsmith/code/infra/platform/kubernetes/backups/aws-s3/README.md`, `VALIDATION.md`
- `/Users/grahamsmith/code/infra/platform/kubernetes/{kopia,nordvpn-meshnet,rclone-gdrive,route53-ddns,tailscale,wikijs}/README.md` (small app READMEs — spot-checked)
- `/Users/grahamsmith/code/infra/platform/wireguard/README.md`
- `/Users/grahamsmith/code/infra/infra/terraform/aws/iam-policies/README.md`, `MIGRATION_PLAN.md`
