[defaults]
inventory = inventory/
host_key_checking = False
deprecation_warnings = False

[ssh_connection]
# ControlMaster disabled to avoid conflicts with ProxyJump
# Use direct connections which are more reliable for remote management
ssh_args = -o ControlMaster=no -o StrictHostKeyChecking=no
pipelining = True
