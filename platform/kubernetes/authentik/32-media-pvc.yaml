apiVersion: v1
kind: PersistentVolumeClaim
metadata: { name: authentik-media, namespace: authentik }
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: ceph-rbd
  resources: { requests: { storage: 2Gi } }
