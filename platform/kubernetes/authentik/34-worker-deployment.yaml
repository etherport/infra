apiVersion: apps/v1
kind: Deployment
metadata:
  name: authentik-worker
  namespace: authentik
  labels: { app.kubernetes.io/name: authentik, app.kubernetes.io/component: worker }
spec:
  replicas: 1
  selector: { matchLabels: { app.kubernetes.io/name: authentik, app.kubernetes.io/component: worker } }
  template:
    metadata:
      labels: { app.kubernetes.io/name: authentik, app.kubernetes.io/component: worker }
    spec:
      enableServiceLinks: false
      securityContext: { runAsNonRoot: true, runAsUser: 1000, runAsGroup: 1000, fsGroup: 1000, seccompProfile: { type: RuntimeDefault } }
      initContainers:
        - name: create-db
          image: ghcr.io/cloudnative-pg/postgresql:16.4
          command: ["sh","-c","psql -v ON_ERROR_STOP=1 -tAc \"SELECT 1 FROM pg_database WHERE datname='authentik'\" | grep -q 1 || psql -v ON_ERROR_STOP=1 -c \"CREATE DATABASE authentik OWNER authentik\""]
          env:
            - { name: PGHOST, value: postgres-cluster-rw.postgres.svc.cluster.local }
            - { name: PGUSER, value: authentik }
            - { name: PGDATABASE, value: postgres }
            - { name: PGPASSWORD, valueFrom: { secretKeyRef: { name: authentik-secret, key: AUTHENTIK_POSTGRESQL__PASSWORD } } }
          securityContext: { allowPrivilegeEscalation: false, capabilities: { drop: ["ALL"] }, readOnlyRootFilesystem: true }
      containers:
        - name: worker
          image: ghcr.io/goauthentik/server:2026.5.3
          args: ["worker"]
          envFrom:
            - configMapRef: { name: authentik-env }
            - secretRef: { name: authentik-secret }
          securityContext: { allowPrivilegeEscalation: false, capabilities: { drop: ["ALL"] } }
          # M114: the worker had NO probes — a wedged worker (blueprint apply,
          # outpost sync, scheduled tasks all silently stop) was invisible.
          # `ak healthcheck` is authentik's documented worker health command.
          # H44 (2026-07-02): startupProbe so a long migration (the worker can be the
          # one that acquires the migration advisory lock) isn't killed by liveness
          # mid-run — 60×10s = up to 10min. Mirrors the server startupProbe.
          startupProbe:
            exec: { command: ["ak", "healthcheck"] }
            periodSeconds: 10
            failureThreshold: 60
            timeoutSeconds: 10
          livenessProbe:
            exec: { command: ["ak", "healthcheck"] }
            initialDelaySeconds: 60
            periodSeconds: 30
            timeoutSeconds: 10
          readinessProbe:
            exec: { command: ["ak", "healthcheck"] }
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 10
          resources: { requests: { cpu: 100m, memory: 384Mi }, limits: { cpu: "1", memory: 768Mi } }
          volumeMounts:
            - { name: media, mountPath: /media }
            - { name: blueprints, mountPath: /blueprints/custom, readOnly: true }
      volumes:
        - { name: media, emptyDir: {} }   # worker media is transient (server holds the PVC)
        - { name: blueprints, configMap: { name: authentik-blueprints } }
