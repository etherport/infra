apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: kube-system-daily
  namespace: velero
spec:
  schedule: "12 3 * * *"   # M119: staggered (was 3:00 sharp with others)
  template:
    includedNamespaces:
      - kube-system
    defaultVolumesToFsBackup: true
    ttl: 720h0m0s
