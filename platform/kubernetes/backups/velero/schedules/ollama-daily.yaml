apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: ollama-daily
  namespace: velero
spec:
  schedule: "30 6 * * *"   # M145: heavy/light interleave, 30-min spacing (heavy (model files))
  template:
    includedNamespaces:
      - ollama
    defaultVolumesToFsBackup: true
    ttl: 720h0m0s
