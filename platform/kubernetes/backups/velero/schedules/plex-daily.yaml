apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: plex-daily
  namespace: velero
spec:
  schedule: "0 3 * * *"
  template:
    includedNamespaces:
      - plex
    defaultVolumesToFsBackup: true
    ttl: 720h0m0s
