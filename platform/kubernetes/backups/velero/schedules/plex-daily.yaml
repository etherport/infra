apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: plex-daily
  namespace: velero
spec:
  schedule: "30 5 * * *"   # M145: heavy/light interleave, 30-min spacing (heavy (config PVC))
  template:
    includedNamespaces:
      - plex
    defaultVolumesToFsBackup: true
    ttl: 720h0m0s
