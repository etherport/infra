apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: plex-daily
  namespace: velero
spec:
  schedule: "24 3 * * *"   # M119: staggered (was 3:00 sharp with others)
  template:
    includedNamespaces:
      - plex
    defaultVolumesToFsBackup: true
    ttl: 720h0m0s
