apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: postgres-daily
  namespace: velero
spec:
  schedule: "40 2 * * *"   # M119: staggered (was 2:00 sharp with others)
  template:
    includedNamespaces:
    - postgres
    defaultVolumesToFsBackup: true
    ttl: 720h0m0s
