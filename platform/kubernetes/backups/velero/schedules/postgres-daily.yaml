apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: postgres-daily
  namespace: velero
spec:
  schedule: "30 1 * * *"   # M145: heavy/light interleave, 30-min spacing (heavy (db))
  template:
    includedNamespaces:
    - postgres
    defaultVolumesToFsBackup: true
    ttl: 720h0m0s
