apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: technitium-daily
  namespace: velero
spec:
  schedule: "48 3 * * *"   # M119: staggered (was 3:00 sharp with others)
  template:
    includedNamespaces:
      - dns
    defaultVolumesToFsBackup: true
    ttl: 720h0m0s
