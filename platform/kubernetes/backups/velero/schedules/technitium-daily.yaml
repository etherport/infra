apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: technitium-daily
  namespace: velero
spec:
  schedule: "0 3 * * *"   # M145: heavy/light interleave, 30-min spacing (light)
  template:
    includedNamespaces:
      - dns
    defaultVolumesToFsBackup: true
    ttl: 720h0m0s
