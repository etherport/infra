apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: technitium-daily
  namespace: velero
spec:
  schedule: "0 3 * * *"
  template:
    includedNamespaces:
      - dns
    defaultVolumesToFsBackup: true
    ttl: 720h0m0s
