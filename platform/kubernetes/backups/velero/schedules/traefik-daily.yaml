apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: traefik-daily
  namespace: velero
spec:
  schedule: "0 2 * * *"   # M145: heavy/light interleave, 30-min spacing (light)
  template:
    includedNamespaces:
      - traefik
    defaultVolumesToFsBackup: true
    ttl: 720h0m0s
