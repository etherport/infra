apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: traefik-daily
  namespace: velero
spec:
  schedule: "20 2 * * *"   # M119: staggered (was 2:00 sharp with others)
  template:
    includedNamespaces:
      - traefik
    defaultVolumesToFsBackup: true
    ttl: 720h0m0s
