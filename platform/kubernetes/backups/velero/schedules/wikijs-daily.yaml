apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: wikijs-daily
  namespace: velero
spec:
  schedule: "0 4 * * *"   # M145: heavy/light interleave, 30-min spacing (light)
  template:
    includedNamespaces:
    - wikijs
    defaultVolumesToFsBackup: true
    ttl: 720h0m0s
