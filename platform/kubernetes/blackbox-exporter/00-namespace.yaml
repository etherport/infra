---
apiVersion: v1
kind: Namespace
metadata:
  name: blackbox-exporter
  labels:
    pod-security.kubernetes.io/enforce: privileged
    # blackbox-exporter probe modules need NET_RAW capability for ICMP,
    # so the pod runs above baseline PSS. HTTPS-only modules wouldn't,
    # but we want both available.
