apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: cloudflare-ddns

resources:
  - namespace.yaml
  - rbac.yaml
  - configmap.yaml
  - cronjob.yaml
  # Migrated 2026-05-27 from 01-route53-secret.sops.yaml (AWS creds for
  # Route53 — zone deleted) to a Cloudflare API token. Old SOPS file
  # left in tree as historical reference; delete after Flux pruning
  # cycles + AWS access key revocation per cloudflare-ddns-cf-migration.md.
  - cloudflare-credentials.sops.yaml
