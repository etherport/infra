apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - 00-namespace.yaml
  - 01-cluster.yaml
  - 02-tailscale-svc.yaml
  - 04-scheduled-backup.yaml # daily base backup → S3   # tailnet-only exposure of the primary (Tailscale operator)
