# --- Basic components ---
prometheus:
  enabled: true
  prometheusSpec:
    # Discover all ServiceMonitors regardless of labels
    serviceMonitorSelectorNilUsesHelmValues: false
    podMonitorSelectorNilUsesHelmValues: false
    storageSpec:
      volumeClaimTemplate:
        spec:
          storageClassName: ceph-rbd
          accessModes: ["ReadWriteOnce"]
          resources:
            requests:
              storage: 10Gi

alertmanager:
  enabled: true
  alertmanagerSpec:
    storage:
      volumeClaimTemplate:
        spec:
          storageClassName: ceph-rbd
          accessModes: ["ReadWriteOnce"]
          resources:
            requests:
              storage: 5Gi

grafana:
  enabled: true

  # Admin login (change this later)
  adminUser: admin
  adminPassword: "ChangeMe123!"

  # Enable sidecar for dashboard provisioning from ConfigMaps
  sidecar:
    dashboards:
      enabled: true
      label: grafana_dashboard
      labelValue: "1"
      searchNamespace: ALL

  # Persist Grafana dashboards & config
  persistence:
    enabled: true
    storageClassName: ceph-rbd
    accessModes: ["ReadWriteOnce"]
    size: 5Gi

  # Expose Grafana via Traefik
  ingress:
    enabled: true
    ingressClassName: traefik
    annotations: {}
    hosts:
      - grafana.wind.etherport.net
    path: /
    pathType: Prefix
    # No tls section here; Traefik will terminate TLS using the wildcard cert
