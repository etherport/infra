# Recipient + subject for the daily service-status report.
# SMTP credentials are pulled from the alertmanager-smtp-config Secret
# (already in this namespace) so no AWS or SES creds need to live here.
EMAIL_TO=graham.m.smith@me.com
EMAIL_SUBJECT=Homelab status
