apiVersion: apps/v1
kind: Deployment
metadata:
  name: ollama
  namespace: ollama
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ollama
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: ollama
      annotations:
        backup.velero.io/backup-volumes-excludes: ollama-data
    spec:
      containers:
        - name: ollama
          image: ollama/ollama:0.24.0 # {"$imagepolicy": "flux-system:ollama"}
          ports:
            - containerPort: 11434
              name: http
          env:
            - name: OLLAMA_HOST
              value: "0.0.0.0"
            # Keep models loaded longer for better responsiveness
            - name: OLLAMA_KEEP_ALIVE
              value: "24h"
          resources:
            # The namespace-default LimitRange (policy-baseline) caps
            # memory at 4Gi for pods that don't set their own limit.
            # Ollama legitimately needs 8Gi+ to load real models, so
            # we set explicit limits here — the request stays 8Gi and
            # the limit gives headroom for whatever model is loaded
            # plus the inference working set.
            limits:
              nvidia.com/gpu.shared: 1
              memory: "12Gi"
            requests:
              memory: "8Gi"
              cpu: "2"
          volumeMounts:
            - name: ollama-data
              mountPath: /root/.ollama
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 30
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
      volumes:
        - name: ollama-data
          persistentVolumeClaim:
            claimName: ollama-data
      # Schedule on GPU node
      nodeSelector:
        nvidia.com/gpu.present: "true"
      tolerations:
        - key: nvidia.com/gpu
          operator: Exists
          effect: NoSchedule
