apiVersion: v1
kind: Service
metadata:
  name: plex
  namespace: plex
  labels:
    app: plex
spec:
  type: LoadBalancer
  loadBalancerIP: 10.10.201.74
  ports:
    - name: plex
      port: 32400
      targetPort: 32400
      protocol: TCP
  selector:
    app: plex
