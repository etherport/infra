apiVersion: v1
kind: Service
metadata:
  name: plex
  namespace: plex
  labels:
    app: plex
spec:
  type: ClusterIP
  ports:
    - name: plex
      port: 32400
      targetPort: 32400
      protocol: TCP
  selector:
    app: plex
