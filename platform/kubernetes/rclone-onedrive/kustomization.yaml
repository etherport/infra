apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: rclone

resources:
  - 01-sync-script-configmap.yaml
  - 02-cronjob.yaml
  - 03-prometheus-rules.yaml
  # 04-secret.sops.yaml — uncomment once the OneDrive OAuth token exists (see
  # README). Until then this component is intentionally NOT registered in
  # clusters/wind/kustomization.yaml, so nothing deploys without its secret.
  # - 04-secret.sops.yaml
