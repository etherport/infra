apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: traefik-ceph-pvc
  namespace: traefik
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: ceph-rbd