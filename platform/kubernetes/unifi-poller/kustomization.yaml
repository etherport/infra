apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: unifi-poller
# LIVE (M55) — wired into clusters/wind/kustomization.yaml; the UniFi View-Only
# account + SOPS secret (01-secret.sops.yaml) are populated and the deployment runs.
# (History: was operator-gated on creating the UniFi account; activated 2026-06.)
resources:
  - 00-namespace.yaml
  - 01-secret.sops.yaml
  - 02-deployment.yaml
  - 03-service.yaml
  - 04-servicemonitor.yaml
