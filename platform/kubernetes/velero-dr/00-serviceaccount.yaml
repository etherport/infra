---
# M137 Phase 3: identity for the velero DR sync (Garage → S3 Deep Archive).
# Trusted by IRSA role wind-irsa-velero (see infra/terraform/aws/cluster-irsa)
# which grants write to s3://velero.wind.etherport.net/* (incl. the dr/ prefix).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: velero-dr-sync
  namespace: backups
