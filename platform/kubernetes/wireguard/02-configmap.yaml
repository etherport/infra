apiVersion: v1
kind: ConfigMap
metadata:
  name: wireguard-config
  namespace: wireguard
data:
  README: |
    WireGuard VPN Configuration
    ===========================

    This pod runs WireGuard with two interfaces:
    - wg0: Site-to-site tunnel to vpn-aws (10.255.255.2/30)
    - wg1: Remote access VPN for mobile clients (10.254.0.1/24)

    VIP: 10.10.201.20 (managed by Keepalived)

    Failover:
    - K8s pod is primary (priority 150)
    - vpn-local VM is backup (priority 100)
    - Keepalived moves VIP automatically on failure
